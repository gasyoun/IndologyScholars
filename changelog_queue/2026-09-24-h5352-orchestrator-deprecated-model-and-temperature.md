- **Philology orchestrator: deprecated Anthropic default bumped and `temperature` dropped from the Anthropic branch (H5352, Claude Code Opus 5 `claude-opus-5`, 24-09-2026):** [`philology-research-agents/orchestrator.py`](https://github.com/gasyoun/IndologyScholars/blob/main/philology-research-agents/orchestrator.py) defaulted to `claude-sonnet-4-20250514`, a Deprecated model. The default is now `claude-sonnet-5` — and the bump alone would have broken the call, because Sonnet 5 rejects `temperature`/`top_p`/`top_k` with a 400, so `temperature=TEMPERATURE` is gone from the `messages.create()` call. The OpenAI-compatible branch keeps `temperature=0.0` unchanged. The two places that printed the temperature into the run header now say `n/a (Anthropic)` on that provider rather than reporting a value that is no longer sent. [`SPINOUT_PLAN.md`](https://github.com/gasyoun/IndologyScholars/blob/main/philology-research-agents/SPINOUT_PLAN.md) config section updated to match (`claude-sonnet-5` / `claude-opus-5`). Verified with `python orchestrator.py --dry-run` (all 6 agents load) — no live API smoke call, since no `ANTHROPIC_API_KEY` is set on the executing box. Source: [claude-config PROMPT_AUDIT_INSTRUCTION_ESTATE_AND_API_CODE_23-09-2026.md](https://github.com/gasyoun/claude-config/blob/main/docs/PROMPT_AUDIT_INSTRUCTION_ESTATE_AND_API_CODE_23-09-2026.md) hunk F4.

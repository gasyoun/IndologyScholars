# Роадмап разделов «Санскритология в России» и «Индология в России»

_Created: 10-07-2026 · Last updated: 10-07-2026 (A1 решено, Фаза 2 первый транш выполнен)_

Проектный план двух новых разделов сайта [IndologyScholars](https://github.com/gasyoun/IndologyScholars),
построенных по образцу [«Дравидология в России»](https://iocs.hse.ru/news/395622778.html) —
авторского сайта А. М. Дубянского при ИВКА НИУ ВШЭ (открыт 31 августа 2020 г.),
который собирает «сведения о зарождении и развитии этого раздела индологии в России»:
люди, их деятельность, достижения, хроника, библиография.

Направление задано интервью с редактором 10-07-2026. Составлено Opus 4.8 (`claude-opus-4-8`).

## Зафиксированные решения

| # | Развилка | Решение |
| --- | --- | --- |
| R1 | Площадка | Разделы живут внутри [IndologyScholars](https://gasyoun.github.io/IndologyScholars/), поверх существующего движка, БД и LOD-графа |
| R2 | Хронологический охват | Полная история: XVIII в. — 2026 г. |
| R3 | Жанр | Гибрид: авторский очерк + врезки, генерируемые из базы данных |
| R4 | Живые лица | Мемориальный формат для ушедших; сухая карточка-реестр для живущих |

Из R1–R4 вытекает пятое решение, архитектурное, которое я принимаю здесь как следствие,
а не как отдельный вопрос:

**R5. «Индология в России» — зонтичный раздел; «Санскритология в России» — фасет внутри него,
а не вторая база данных.** Один спайн персон, один спайн трудов, одно поле дисциплины.
Два раздела — это два *представления* над одним графом. Дубянский трактует дравидологию
именно так — как «раздел индологии», — и то же отношение связывает санскритологию с индологией.
Альтернатива (две независимые базы) немедленно раздвоила бы Щербатского, Оболенского,
Иванова и ещё сотню людей, работавших в обеих рамках.

**A1 (решено человеком 10-07-2026, вариант A). Где живут исторические фигуры, не выступавшие
на чтениях.** В `conferences.db` ноль персон без докладов: человек попадал в `person` только
через парсер программ. Прямая заливка ~40 исторических фигур сломала бы цитируемое «268
докладчиков». Ruling: **один спайн `person` с колонкой-дискриминатором `person_kind`**
(`conference_participant` | `historical`) — прямое следствие R5, а не второй граф. Все
публикуемые счётчики докладчиков считаются через соединение с `presentation_person`, а не
`count(person)`, поэтому 268 остаётся 268. Отвергнуты: отдельная таблица `historical_person`
(раздвоила бы спайн вопреки R5) и расширение `RIND_`-реестра (фасетные таблицы Фазы 1
ссылаются на `person(person_id)`). Реализация — [H484](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H484-Opus_IndologyScholars_historical-prosopography-phase2_10.07.26.md).

## Что у нас есть и чего нет

Замер по [`conferences.db`](https://github.com/gasyoun/IndologyScholars/blob/main/conferences.db)
на 10-07-2026 (`origin/main`, коммит `733858b10`):

| Показатель | Значение | Что это значит для разделов |
| --- | ---: | --- |
| Персон в БД | 268 | Расхождение с «270» разъяснено в Фазе 1, см. риск P4 |
| Из них с годом рождения | 234 (87%) | Достаточно для генераций |
| Из них с годом смерти | 26 (10%) | **26 кандидатов на мемориальный очерк** — вся ближняя перспектива R4 |
| Из них с учёной степенью | 35 (13%) | Слишком мало для институциональной картины |
| Самый ранний год рождения | **1918** | **Ключевой факт роадмапа** |
| Диапазон событий | 2004–2026 | Корпус — срез, а не история |
| Связей с Wikidata | 1 | Контроль авторитетов практически отсутствует |
| Связей с Wikipedia | 5 | То же |
| Записей `data_assertion` | 803 | Механизм «факт + источник + уверенность» уже работает |

Отсюда честный вывод, который должен быть в основании всего плана:
**в корпусе нет ни одного человека, родившегося до 1918 года.** Решение R2 (полная
история с XVIII в.) означает, что весь слой от Герасима Лебедева и Роберта Ленца до
Ольденбурга и Щербатского строится с нуля. Существующие 268 профилей покрывают
последнюю четверть истории дисциплины и ноль процентов первых двух столетий.
Это не повод отказываться от R2 — это повод не притворяться, будто раздел
«почти готов, осталось оформить».

## Разрыв в модели данных

Нынешняя схема — конференц-центричная: `event_series → event → event_day → session →
presentation → presentation_person`. Она отлично описывает, кто когда где выступил.
Она не описывает **научную биографию**, а именно её требует жанр Дубянского.

Отсутствуют три сущности, без которых раздел не собрать:

- **`work`** — труд: книга, статья, перевод, словарь, издание текста. Сейчас в базе нет
  публикаций вообще, только доклады. Библиография — половина сайта-образца.
- **`person_role`** — должность/аффилиация во времени: `person × organization × from_year ×
  to_year × role`. Сейчас аффилиация висит строкой на конкретном докладе
  (`presentation_person.affiliation_text_raw`), то есть не существует вне доклада.
  Для человека, умершего в 1942 г., докладов не будет никогда.
- **`discipline`** — фасет на персоне и на труде, реализующий R5.

Плюс два расширения того, что уже есть:

- **`relation`** (учитель–ученик, преемство кафедры) — частично лежит в
  [`curation/`](https://github.com/gasyoun/IndologyScholars/tree/main/curation),
  но не поднято в схему.
- **`narrative`** — авторские очерки как Markdown, привязанные к сущностям по идентификатору;
  именно они дают букву «очерк» в R3.

Хорошая новость: таблица
[`data_assertion`](https://github.com/gasyoun/IndologyScholars/blob/main/build_and_populate_db.py)
(803 строки, поля `source_url`, `citation`, `confidence`, `curator_id`, `verified_at`)
— ровно тот механизм, который нужен историческому слою, где каждый факт спорен и
требует ссылки. Её не надо изобретать, надо на неё опереться.

## Фазы

Порядок выбран так, чтобы раздел можно было открыть до того, как будет готов
исторический слой, — иначе публикация откладывается на год.

### Фаза 0 — правовой и терминологический триаж (дни, до кода)

Прежде чем собирать биографии, надо знать, из чего их законно собирать.

- Реестр источников с колонкой прав. Главный вопрос — **Милибанд**,
  «Биобиблиографический словарь отечественных востоковедов» (1995, 2 т.): это
  единственный полный справочник и он под копирайтом примерно до 2081 г.
  Факты (даты, должности, названия трудов) не охраняются; текст статей охраняется.
  Практический режим: использовать как **сверочный** источник, ссылаться на страницу,
  никогда не переносить формулировки. То же для «Истории отечественного
  востоковедения» (Наука).
- Public-domain слой: некрологи и отчёты до 1930 г., ЗВОРАО, «Записки Восточного
  отделения», собственные библиографии Ольденбурга, дореволюционные университетские
  обозрения преподавания.
- Таксономия дисциплин v0 — см. открытый вопрос D1.

Аналог уже отработан: в [AfanasiyNikitin](https://github.com/gasyoun/AfanasiyNikitin)
триаж прав на тексты предшествовал сбору данных, и это себя оправдало.

### Фаза 1 — спайн разделов (handoff H473) — ✅ выполнена 10-07-2026

Открывает оба раздела на существующих данных, без исторического слоя.

- Схема: `work`, `person_role`, `discipline`, `relation` — миграция
  [`build_and_populate_db.py`](https://github.com/gasyoun/IndologyScholars/blob/main/build_and_populate_db.py).
- Проставить фасет `discipline` 268 персонам — **вручную/полуавтоматически**.
  См. риск P1: существующий классификатор ключевых слов использовать нельзя.
- Две посадочные страницы: `indologiya-v-rossii.html`, `sanskritologiya-v-rossii.html`,
  генерируемые из БД, с врезками (счётчики, генерации, карта городов) поверх
  редакционного текста-заглушки.
- Раздвоение шаблона персоны по `death_year`: непустой → мемориальный очерк,
  пустой → карточка-реестр. Это прямая реализация R4 в
  [`generate_scholars_pages.py`](https://github.com/gasyoun/IndologyScholars/blob/main/generate_scholars_pages.py)
  и [`templates/scholar_detail.html`](https://github.com/gasyoun/IndologyScholars/blob/main/templates/scholar_detail.html).
- ~~Сверить 268 против 270~~ — сделано, см. риск P4.

Результат: раздел существует, честно ограничен 2004–2026, и его уже можно показывать.

### Фаза 2 — исторический просопографический слой (первый транш выполнен 10-07-2026)

Первый транш загружен: **26 фигур** XVIII — середины XX в. (Лебедев, Ленц, Коссович,
Петров, Бётлингк, Васильев, Минаев, Миллер, Овсянико-Куликовский, Ольденбург, Щербатской,
Сталь-фон-Гольштейн, Мерварт, Баранников, Смирнов, Тюляев, Обермиллер, Востриков, Рерих,
Ульяновский, Кальянов, Антонова, Бродов, Гусева, Макаев, Серебряков). Источник истины —
[`curation/historical_persons.csv`](https://github.com/gasyoun/IndologyScholars/blob/main/curation/historical_persons.csv)
(+ роли в [`historical_person_roles.csv`](https://github.com/gasyoun/IndologyScholars/blob/main/curation/historical_person_roles.csv)),
сидер [`pipeline/historical.py`](https://github.com/gasyoun/IndologyScholars/blob/main/pipeline/historical.py).

- **Сеть.** Wikidata REST и ru.wikipedia оказались доступны с хоста 10-07-2026 (вопреки
  записи в `.ai_state.md` о таймаутах), поэтому даты, Q-ID и институции взяты напрямую из
  Wikidata резолвером [`tools/resolve_historical_wikidata.py`](https://github.com/gasyoun/IndologyScholars/blob/main/tools/resolve_historical_wikidata.py),
  а не через мост enwiki. Резолвер помечает расхождения дат (`status=disputed`): у Вострикова
  и Коссовича год рождения на Wikidata спорен.
- Каждый биографический факт — строкой в `data_assertion` (`curator_id='h484_historical_seeder'`)
  с `source_url` на страницу Wikidata. Ноль фактов без ссылки. 78 строк.
- `person_role` заполнен из Wikidata P108/P69: **82 строки** (16 с датами-квалификаторами),
  `organization_id` пока NULL (как в Фазе 1), лейбл организации — в `notes`.
- Дисциплины проставлены вручную по биографии в
  [`curation/person_disciplines.csv`](https://github.com/gasyoun/IndologyScholars/blob/main/curation/person_disciplines.csv)
  (`method=manual_biographical`): у исторических фигур нет докладов, meso-кроссволк к ним неприменим.
- 17 дореволюционных строк из `non_participant_indologists.csv` **перенесены, а не
  продублированы**: реестр `indologists.html` их больше не показывает (у них теперь свои
  мемориальные страницы). Попутно исправлен неверный Q-ID Антоновой (`Q12486524` →
  индолог `Q19933264`).

Остаётся: добор до полного ~40-фигурного транша (имперский период) и последующие фигуры
итеративно.

### Фаза 3 — авторский нарратив (недели, параллельно Фазе 2)

- Очерк истории в 6–8 периодах, от первых кафедр до наших дней. Это буква «очерк» в R3
  и то, чего нет ни в одной базе данных.
- Мемориальные очерки: 26 ушедших из нынешнего корпуса + фигуры Фазы 2.
- Отдельный очерк «Санскритология в России» — с опорой на наш собственный
  лексикографический материал: словарь Коссовича, русские переводы PWG, история
  преподавания санскрита. Здесь у нас есть то, чего нет ни у кого другого, — см. Фазу 4.

### Фаза 4 — библиографический спайн и связка с лексикографией (недели)

- Наполнить `work`: труды, а не доклады. Начать с санскритологического фасета,
  где источник под рукой — репозиторий
  [SanskritLexicography](https://github.com/gasyoun/SanskritLexicography).
- Связать раздел «Санскритология в России» с реальными оцифрованными артефактами
  (словарь Коссовича, слои PWG→RU). Это превращает раздел из мемориала в рабочий
  инструмент и отличает его от сайта-образца.

### Фаза 5 — публикация (дни, после Фазы 3)

- `CITATION.cff`, DOI на срез данных, страница «как цитировать» — механика уже есть
  в репозитории.
- Перекрёстная ссылка с «Дравидологией в России»; см. открытый вопрос D5.
- Гейт [`/publish-safety-check`](https://github.com/gasyoun/claude-config/blob/main/commands/publish-safety-check.md)
  до анонса — раздел содержит данные о живущих людях.

## Открытые вопросы — решает человек

Эти пять развилок я не задал в интервью; они не блокируют Фазы 0–1, но блокируют Фазу 2.

| ID | Вопрос | Почему это важно |
| --- | --- | --- |
| D1 | Границы «индологии»: входят ли тамилология, буддология, тибетология, непалистика, история и археология Индии? **Плюс, по итогам Фазы 1:** ратифицировать ли четыре кода со `status=proposed` — `literature`, `linguistics`, `ethnography`, `history_of_indology`? | Определяет и таксономию `discipline`, и то, кого вообще включать. Дубянский считает дравидологию разделом индологии — симметрия обязывает. **Фаза 1 не заблокировалась:** стартового набора из 9 кодов хватило лишь на 203 из 268 персон, поэтому добавлены 4 кода с пометкой `proposed` в [`curation/disciplines.csv`](https://github.com/gasyoun/IndologyScholars/blob/main/curation/disciplines.csv). Справочник плоский, с `parent_discipline_id`, так что решение D1 меняет строки, а не схему |
| D2 | Кто пишет нарратив: редактор лично, приглашённые авторы, или ИИ-черновик под редактурой с явной атрибуцией? | Определяет сроки Фазы 3 и подпись под очерками |
| D3 | Язык: только русский, или русский + английский? | В репозитории уже двуязычная инфраструктура; удвоение стоимости нарратива |
| D4 | Милибанд: сверяться с ним по фактам со ссылкой на страницу — или не открывать вовсе? | Правовой и репутационный риск против полноты |
| D5 | Кооперация с ИВКА НИУ ВШЭ и взаимная ссылка с «Дравидологией в России»? | Меняет статус проекта с частного на партнёрский |

## Риски

| ID | Риск | Смягчение |
| --- | --- | --- |
| P1 | **Классификатор ключевых слов дефектен.** Незаякорённые кириллические основы в [`keyword_filtering.py`](https://github.com/gasyoun/IndologyScholars/blob/main/keyword_filtering.py): основа «тика» срабатывает внутри «Эротика», «практика». Ошибочно ≥7,1% совпадений — это пол, а не оценка ([docs/renou-precision-audit.md](https://github.com/gasyoun/IndologyScholars/blob/main/docs/renou-precision-audit.md)) | Фасет `discipline` в Фазе 1 **не выводить** из этих основ. Сначала дождаться [H459](https://github.com/gasyoun/Uprava/blob/main/handoffs/H459-Sonnet_IndologyScholars_renou-rules-anchor-fix-and-dedupe_10.07.26.md) (якорение основ + дедуп), затем перепроверить. До того — ручная разметка 268 персон |
| P2 | Рабочее дерево `IndologyScholars` содержит 1590 изменённых файлов на ветке `codex/indology-archive-atlas-v0.1.0` — идёт параллельная сессия | Вся работа по H473 ведётся в отдельном worktree от `origin/main`. Не коммитить в общее дерево |
| P3 | Мемориальный жанр требует `death_year`, заполненного у 26 из 268 | Не выводить «жив/умер» из отсутствия данных. Пустой `death_year` означает «неизвестно», и шаблон обязан трактовать его как реестр, а не как мемориал. Для исторических персон Фазы 2 поле обязательно |
| P4 | ~~README заявляет 270 персон, БД содержит 268~~ — **снят 10-07-2026**; посылка риска сама оказалась устаревшей: README уже заявлял 268 | Расхождение объяснено. Коммит [`52b05255f`](https://github.com/gasyoun/IndologyScholars/commit/52b05255f) (30-06-2026) пересобрал БД и в том же коммите поправил README. Две курируемые склейки личностей (Котин В. В. → Игорь Юрьевич; Бушуев Е. А. → Евгений Сергеевич — обе в уже существовавшие полные профили) плюс три переименования карточек-инициалов дают −5 +3 = −2. [`validate_publication.py`](https://github.com/gasyoun/IndologyScholars/blob/main/validate_publication.py) жёстко сверяет README с `summary.total_scholars`, поэтому 268 — проверяемая величина, а не декларация. Устаревшие «270» остались лишь во вторичных документах и вычищены |
| P5 | Раздел о живущих коллегах в маленьком поле — конфликт интересов | R4 плюс существующая [политика персональных данных](https://github.com/gasyoun/IndologyScholars/blob/main/docs/persons-data-policy-ru.md): никаких оценок вклада живущих, только факты из опубликованных источников |

## Что наши данные дают такого, чего нет у сайта-образца

Это ответ на вопрос «зачем повторять чужой проект». Не повторять — у нас другой рычаг.
Сайт Дубянского — авторский текст. У нас под текстом лежит граф, и из него генерируются
врезки, невозможные в жанре очерка:

- пересечение когорт Зографских и Рериховских чтений (41 человек в обеих);
- траектории аффилиаций во времени — кто куда переходил;
- сеть соавторства и соприсутствия в программах;
- поколенческие срезы по году рождения (234 значения);
- география: 13 городов, от Петербурга до Улан-Удэ и Элисты;
- LOD-выгрузка [`indology_knowledge_graph.ttl`](https://github.com/gasyoun/IndologyScholars/blob/main/indology_knowledge_graph.ttl) —
  раздел цитируем и переиспользуем машиной.

Именно это делает R3 (гибрид) не компромиссом, а единственным форматом, в котором
наш материал сильнее исходного образца.

## Запуск

Фаза 1 исполняема немедленно, Фазы 0 и 1 не зависят друг от друга по коду
(Фаза 0 — правовой триаж, Фаза 1 — схема и шаблоны).

```
Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H473-Opus_IndologyScholars_sections_spine_phase1_10.07.26.md and execute it.
```

_Dr. Mārcis Gasūns_

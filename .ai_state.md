# Project Objective: ППВ подан → интернационализация данных + спутники корпуса

Статус на 2026-07-23: **ППВ подан; A26 data paper deposit-ready; спутники
(nagari, vk-ors) опубликованы/влиты.** Активный трек — human gates (Zenodo
deposit, OpenAlex review, IRR) и Phase-5 enrichment на clean-egress хосте.
Корпус докладчиков: **268 учёных, 1362 уникальных доклада, 1388 авторских
участий, 2004–2026, 41 перекрёстной когорты** (`site_data_summary.json`;
плюс **26** исторических фигур вне счётчика докладчиков). Релиз **v1.4.2**.

## Текущий фокус (приоритет редактора)

1. **Data paper A26** — `article/data_paper_draft.md` → Research Data Journal
   (Brill). Депозит-пакет Zenodo **готов** (H1072): снимок
   `article/snapshots/2026-07-17/`, `article/zenodo_metadata.json`,
   `CITATION.cff` синхронизирован. Остались human-шаги: логин Zenodo → DOI →
   подача в Brill. Human IRR (второй кодировщик) отложен на 2026.
2. **Контроль авторитетов** — **181** персон / **496** строк-кандидатов OpenAlex
   (`analytics_output/openalex_author_candidates.csv`, все `todo`); Wikidata-батч
   (Q-ID исправлены) — только после сверки.
3. **Phase-5 enrichment** — на clean-egress (.ru/VPN) хосте: Wikidata REST +
   ru.wikipedia (достижимость **переменна** по хосту/дню; 10-07-2026 с части
   хостов работала). Ростер неучастников (`indologists.html`, 94) + линкер
   участников уже влиты.

## Зафиксированные решения (обновлено 2026-07-23)

1. **Цель:** международная привязка корпуса и его формальная цитируемость.
2. **Всё через генератор:** `site_data.json` и HTML/CSV/JSON — производные, править генераторы.
3. **Явная неопределённость:** `(?)` для открытых аффилиаций; OpenAlex-совпадения
   входят в `authority_ids.json` только как `confidence='candidate'`, не `confirmed`.
   Порог инъекции — `relevance_score ≥ 0.8` (подтверждён).
4. **Wikidata:** Q-ID исправлены (P106 → `Q18524037` индолог, P101 → `Q625510`
   индология, удалён фиктивный `Q126692818`, источник как референс `S854`).
   Батч в QuickStatements отправляется только после полной ручной сверки
   OpenAlex-кандидатов (181 персон).
5. **Ростер индологов:** **влит** — участники со связью с профилями,
   **неучастники — отдельной страницей-реестром** (`indologists.html`).
6. **Data paper:** целевой журнал — Research Data Journal for the Humanities and
   Social Sciences (Brill); **авторство единоличное (Gasūns)**, инструменты — в
   acknowledgements.
7. **DOI/версия датасета:** **Zenodo** через GitHub-release; версия —
   **датированная на каждый снимок**, `CITATION.cff` бампится при `freeze`.
8. **.ru-шаги:** редактор прогоняет Playwright/ru-инфобоксы/Wikidata REST изнутри
   .ru по **runbook**; парсинг результатов — здесь.
9. **Git workflow:** prefer worktree off `origin/main` (shared-contention clone);
   CI авторебилдит HTML/DB (не править derived вручную).
10. **Рамка PPV-статьи:** статья остаётся исследованием двух главных долгосрочных
   площадок — Зографских и Рериховских чтений. Семенцовские чтения считаются
   новым/маргинальным контекстом вне корпуса; PDF Парибка используется только
   как качественное свидетельство конференционной экологии, не как строковые
   данные для статистики, L1/L2, G1-G3 или перестановочной модели.
11. **INDOLOGY-L atlas** выделен в [`gasyoun/IndologyArchiveAtlas`](https://github.com/gasyoun/IndologyArchiveAtlas)
    (H460); здесь — feed + redirect `/IndologyArchive/`.
12. **Спутники корпуса:** `nagari/` (закрытая гуглгруппа, Pages-ретроспектива),
    `vk-ors/` (VK-стена «Общество ревнителей санскрита», SQLite+FTS5 + 4 слоя).

## ➡️ Next Steps (Queue)

- **H3567 (Opus 5, 🟡2 medium) — meso-discipline crosswalk registered as a feed with named consumers: 🟡 queued, not started.** Staged 26-08-2026 by the `/ask-batch` repo-interconnection pass. Plan: [PLAN_INDOLOGYSCHOLARS_INTERCONNECTION_2026-08.md](https://github.com/gasyoun/IndologyScholars/blob/main/docs/PLAN_INDOLOGYSCHOLARS_INTERCONNECTION_2026-08.md). Programme spec (shared by all 14 repos): [PLAN_SPINE_INTERCONNECTION_2026H2.md](https://github.com/gasyoun/Uprava/blob/main/docs/PLAN_SPINE_INTERCONNECTION_2026H2.md). **Nothing runs until a human launches it** — this is a staged backlog entry, not work in flight.

- **H1895 done (2026-07-31, Sonnet 5 `claude-sonnet-5`)** — see Completed below.
  **H1897 (substantive per-source → shared-axis crosswalk adjudication)** is
  the next open step: `community_lenses/codebooks/{native_topic,shared_topic}.csv`
  and `theme_codes_final_v2.csv`'s l1/l2/l3/l4 thematic codes still need the
  actual crosswalk decision (H1893 shipped only the contract shells; H1895
  deliberately did not pre-empt this). **H1898 (person linkage/quotes)** can
  now consume conferences/nagari/vk_ors adapter output as input (see Completed
  below for exactly what each corpus produced); indology_l stays blocked until
  H1894 (atomic INDOLOGY-L feed hardening) is actually built — it was never
  built despite the registry marking it Done, see Dev Notes. **H1896 (BVP adapter) done
  (2026-07-31, Sonnet 5 `claude-sonnet-5`)** — see Completed below — BVP first-page
  acquisition is landed (`bvp/`); full archive stats still need a separate
  pagination unit before quantitative use. Read
  [IMPLEMENTATION](https://github.com/gasyoun/IndologyScholars/blob/main/docs/IMPLEMENTATION_IndologyScholars_sanskrit-community-lenses.md)
  Steps 6+ and
  [VERIFICATION](https://github.com/gasyoun/IndologyScholars/blob/main/docs/VERIFICATION_IndologyScholars_sanskrit-community-lenses.md)
  gates V1/V3/V5 first for the crosswalk/identity work.
- **Zenodo deposit (human):** залогиниться на Zenodo, выгрузить
  `article/snapshots/2026-07-17/` (или GitHub-release integration), вписать
  concept+version DOI поверх `PENDING` в `article/data_paper_draft.md` §5.4/§7
  и `CITATION.cff`, затем подать A26 в Brill.
- **DeepSeek-прогон (clean-egress хост — .ru или VPN, НЕ текущий хост):** `.env`
  уже несёт `OPENMODEL_*`. Порядок: `python tools/openmodel_client.py --selftest`
  (должен напечатать `OK … reply: 'OK'`) → `python tools/run_kfold_classification.py
  --limit 40` (дешёвый триал) → `--k 5 --temp 0.5` (полный, resumable) →
  `python tools/run_video_segmentation.py --limit 3` → полный (или `--task
  segment_session`). Выходы: `analytics_output/classification_kfold.csv`
  (+`classification_kfold_disagreements.csv`), `analytics_output/video_segment_candidates.csv`.
  Промоутинг high-confidence строк сегментации в `video_presentation_mapping.csv`
  и сведение k-fold с публикуемой классификацией — отдельные ручные шаги.
- **Whisper для 8 видео без caption-дорожек** (`scratch/transcripts/whisper_queue.csv`),
  затем повторить `parse_captions.py` + `build_deepseek_inputs.py`. Проверка
  2026-06-26: локальных media-файлов нет, `yt-dlp` не установлен/не на PATH; очередь
  содержит private/removed/HTTP-429/timeout видео, так что автономно на этом хосте
  запустить Whisper нельзя.
- **IRR (человек):** передать второму кодировщику `analytics_output/interrater_sample_blind.csv` (БЕЗ key-файла!) + инструкцию из `tools/build_interrater_sample.py`; затем `python tools/compute_interrater_agreement.py`. Кросс-модельная сверка (Claude vs DeepSeek) уже посчитана — см. `docs/classification-reliability-packet.md`.
- **Валидация пола:** заполнить `verified_gender` (F/M/X) + `evidence_url` в `analytics_output/gender_validation_sample.csv` (60 строк), затем `python tools/validate_gender_inference.py` — её error rate цитируется в data paper и на `findings/gender.html`.
- **Голосование:** создать Яндекс Форму (email/имя/«JSON выгрузки»), вписать её URL в `VOTE_TEACHER_FORM_URL` (`generate_publication_pages.py`), пересобрать; после занятия — `tools/merge_votes.py responses.csv --json-column "JSON выгрузки"`
- **Phase 5 (.ru, по `docs/ru-enrichment-runbook.md`):** на хосте с рабочим Wikidata REST / ru.wikipedia: `wikidata_enrich.py` (в т.ч. Q-ID с пропущенными датами) → `expand_wikipedia_indologists.py` → ручная курация `candidate`→`verified`
- Ручная сверка **181** персон OpenAlex (`analytics_output/openalex_author_candidates.csv`, 496 строк `todo`→`confirmed`), затем отправка Wikidata-батча в QuickStatements
- Ручной добор имперского периода (~15 имён) в ростер / historical layer

## 🚧 Current WIP

- **H2315 (Sonnet 5 `claude-sonnet-5`, 06-08-2026) — CRLF/CR blob renormalize (org EOL
  census, [Uprava/tools/eol_census.py](https://github.com/gasyoun/Uprava/blob/main/tools/eol_census.py)).**
  1,311 files carried CR bytes in their committed blobs (1,302 under `nagari/md/`,
  plus `article/ppv_draft.md` and `scratch/decode_out.txt`) — a mix of CRLF line
  endings and lone `\r` (old Mac-style) line breaks inside the 2007-era archival
  Google-Group export text. `git add --renormalize -A` fixed the CRLF pairs; a
  follow-up universal-newline pass (`\r\n`→`\n`, then lone `\r`→`\n`) fixed the
  remainder — verified 0 files carry any `\r` byte post-fix, 0 deletions, line
  counts only ever increase (no content merged/lost). Closes
  [IndologyScholars#178](https://github.com/gasyoun/IndologyScholars/issues/178).
  Pushed directly to `main` (no branch protection requiring a PR here).

- **H1899 (Opus 5 `claude-opus-5`, 06-08-2026) — Wave 1E пятилинзового сравнения ВЫПОЛНЕН;
  приземлён ЧАСТИЧНО (PR #171 в `codex/community-lenses-ask-plan`).**
  Публично приземлено: `community_lenses/{metrics,figures,report,snapshot,cli}.py` +
  фикс пути nagari-адаптера, 7 таблиц метрик, 6 рисунков с подписями,
  `comparison_validity.md`, `claims_ledger.csv` (14 утверждений, 0 без свидетельства),
  `article/ppv_comparative_revision_outline_ru.md`, 40 новых тестов
  (по репозиторию 347 passed). Сводка и разбор публикационного сплита:
  `analytics_output/community_lenses/reports/H1899_completion_note.md`.
  **НЕ приземлено и живёт ТОЛЬКО в worktree `IndologyScholars-h1899-34220`:**
  оба пакета `article/comparison_snapshots/` (through-2025 = 27 270 записей,
  partial-2026 = 427; в `records.csv` 18 572 message-ID закрытой группы),
  `curation/community_{person_links,quotes}.csv` и
  `analytics_output/community_lenses/reports/identity_quote_evidence.md` — правовой
  шлюз закрытой группы не снят. **Тот же класс потери данных, что уничтожил BVP-срез
  (FINDINGS §314 / issue #169): этот worktree и `IndologyScholars-h1898-1193884`
  удалять нельзя.** Генератор публичен, поэтому обладатель источников пересобирает оба
  пакета байт-в-байт. Следующий шаг по содержанию — H1900 (правка русской статьи),
  начиная с §2 «Корпус и метод».

- **Релиз по этой работе НЕ нарезан намеренно:** запись CHANGELOG живёт на
  интеграционной ветке `codex/community-lenses-ask-plan`, а не на `main`; тег на
  интеграционную ветку означал бы релиз состояния, которого на `main` нет. `/cut-release`
  делается, когда вся линза-линия вливается в `main`.

- **Известный дефект, ставший громким (issue #169):** после H1899-фикса пути к
  `nagari/data/nagari.db` два теста `tests/test_community_lenses_adapters.py` (nagari)
  падают на дублирующихся Message-ID. Чинить надо в АДАПТЕРЕ (дедупликация при
  извлечении, как уже делает `identity.dedupe_fixture` ниже по потоку), а не удалением
  копии базы — удаление возвращает тихую ложную «unavailable»-линзу.

- Whisper queue remains blocked locally (2026-06-26): 8 queued videos, 0 matching
  local audio/video files under `scratch/`, no `scratch/transcripts/<video_id>.json`
  outputs, and no `yt-dlp` executable on PATH. Needs media files or a host/tooling
  setup that can fetch the remaining videos.

- **openmodel.ai inference часто недоступен с automation-хоста (раннеры готовы, ждут clean egress).**
  Шлюз: `GET /v1/models` = 200, но `POST /v1/chat/completions` с части сетей
  падает (HTTP/2 abrupt-close / gateway 404 / TCP-reset) — DPI/фильтрация
  длинных POST-TLS. Прогонять раннеры с .ru/VPN-хоста.

- **Wikidata REST / ru.wikipedia:** достижимость **host-dependent** (часто RKN-блок
  с automation-хоста; 10-07-2026 H484 historical resolve с другого хоста прошёл).
  Не считать «навсегда down» — проверять перед Phase-5 прогоном.

## 🧠 Dev Notes

- **Как генерируются страницы:** `generate_publication_pages.py` → `page_shell()` → `templates/base.html`.
- **Сборка:** `build_and_populate_db.py` → `check_ppv_numbers.py` → `generate_site_data.py` → `validate_publication.py` → `pytest`.
- **Авторитеты:** `authority_ids.json` — `persons` (openalex/orcid/wikidata + confidence) и `organizations` (wikidata). Гео Q-ID — в `assets/data/geography.json`.
- **OpenAlex pipeline:** `scratch/openalex_author_candidates.py` (или `resume_openalex.py`) → CSV → ручная сверка → `tools/inject_openalex_matches.py` → `authority_ids.json`.
- **Wikidata pipeline:** `tools/generate_wikidata_batch.py` → `analytics_output/wikidata_batch.txt` → QuickStatements → Q-ID обратно в `authority_ids.json`. Руководство — `docs/wikidata-guide.md`.
- **LOD:** `generate_lod.py` → `indology_knowledge_graph.ttl`.
- **Снимок для DOI:** `tools/freeze_article_data.py` → `article/snapshots/<date>/`.
- **Сетевая достижимость (хост автоматизации):** `en.wikipedia` ✅; `ru.wikipedia` API/статьи ❌ (РКН); `wikidata` REST/SPARQL ❌ отсюда; институц. сайты ❌; **YouTube (yt-dlp) ✅**; **openmodel.ai inference ❌** (`GET /v1/models` ✅, но `POST /v1/chat/completions` ❌ — DPI/reset, см. WIP). Wikidata/ru/DeepSeek-зависимые шаги запускать там, где есть доступ.
- **openmodel.ai шлюз:** host = `api.openmodel.ai` (НЕ `.app` — тот не резолвится; `.app` был ошибочным чтением доков), base `/v1`, OpenAI-совместимый. Модели DeepSeek: `deepseek-v4-flash` (дешёвая, bulk) и `deepseek-v4-pro`; также Claude/GPT-5.x/Gemini-3.x/Qwen/GLM/Kimi/MiniMax (кросс-модельная сверка). Ключ формата `om-…`. `.env`: `OPENMODEL_API_KEY/OPENMODEL_BASE_URL/OPENMODEL_MODEL`; раннеры читают их с фоллбэком на `DEEPSEEK_*`, legacy-классификатор `article/work_expanded_classification_deepseek.py` по-прежнему на `DEEPSEEK_*`.
- **YouTube-captions пайплайн:** `tools/caption_inventory.py` → `tools/pull_captions.py` → `tools/parse_captions.py` → `tools/build_deepseek_inputs.py`. VTT auto-captions — rolling-window: чистый текст = только строки с inline-таймтегами `<hh:mm:ss.mmm>` (остальное — дубли-переносы); дорожки `ru` и `ru-orig` идентичны (берём `ru-orig`). Транскрипты в `scratch/transcripts/<id>.json` (таймкоды в секундах для чаптеринга).
- **DeepSeek-раннеры (для clean-egress):** `tools/openmodel_client.py` (общий клиент: config, ретраи/бэкофф, `GatewayError` на envelope шлюза вместо зависания, `--selftest`/`--models`); `tools/run_kfold_classification.py` (k=5 self-consistency, **переиспользует** `SYSTEM_PROMPT`/`user_prompt`/`normalize_result` из legacy-классификатора — таксономия НЕ дублируется; голосование по полям + agreement, `temp>0` обязателен иначе прогоны идентичны; пишет в свои файлы, не перетирает `expanded_classification_deepseek.csv`); `tools/run_video_segmentation.py` (align_single/verify_match/segment_session, чанкинг с overlap для 10-часовых сессий, очередь-ревью с `(?)` и `status=auto|todo`).
- **⚠️ H1894 registry-vs-reality discrepancy (found 2026-07-31, Sonnet 5
  `claude-sonnet-5`, while executing H1895):** `Uprava/handoffs/archive/
  H1894-...manifests-indology-feed_29.07.26.md` is filed under `archive/` and
  the org registry marks it Done, but **nothing was actually built** — no
  atomic staging-then-promote fetcher, no pinned `SourceManifest`, no
  `tests/test_indology_feed_snapshot.py`. `tools/fetch_indology_feed.py` is
  still the pre-H1894 naive per-file `urllib.request.urlopen` fetcher with no
  manifest/hash verification and no atomic promotion. Do not trust the
  registry status for this one without re-checking the actual repo state; a
  future session should either re-run H1894 for real or re-file it as
  not-done. H1895's `community_lenses/adapters/indology_l.py` refuses to
  consume this unverified feed (per H1895 requirement 5) and returns an
  explicit `coverage_status="unavailable"` instead.
- **⚠️ Год рождения — источник истины = `pipeline/biography.py:BIOGRAPHICAL_DATA`** (ключ — `normalized_key`), НЕ `conferences.db`. Сборка дропает и пересевает `person` из этого dict (`schema.py:init_db` → `build_and_populate_db.py`), поэтому стойкая правка = добавить запись в `BIOGRAPHICAL_DATA`. Существующая цепочка `scrape_birth_years.py` → `birth_year_findings.csv` → `apply_birth_years.py` пишет ТОЛЬКО в `conferences.db` и затем советует пересборку, которая её же затирает — результат НЕ переживает rebuild. Перед использованием `apply_birth_years.py` надо перенаправить на эмиссию строк `BIOGRAPHICAL_DATA`. **34/268** без года (`missing_birth_years.md`, 2026-07-10) — все настоящий пробел источника. Скаут 2026-06-14: Wikidata+ru.wiki дают **0** автоматических заполнений на длинном хвосте. Аналитически важны **~5 повторных докладчиков**; остальное — в лимитацию (~12.7% missing).
- **Аффилиации ~закрыты (не binding constraint):** из 1388 упоминаний 9 авторитетных орг. дают ~290, `geography.json` — ~620 городов, `«Не указана»` = 345 (несократимо); реальный остаток — низкочастотный хвост ~14 институтов / ~60 упоминаний (≈4%: ГИИ, РУДН, МГППУ, ИСАА, ИМЛИ РАН, ИЯ РАН, ИЭА РАН, ГАУГН, МЦР, ИВ НАН Украины, Ulster Univ.). `normalize_affiliation` в `publication_helpers.py`.

## ✅ Completed

- **H2579 stale-base / CRLF pre-push harden (14-08-2026, Grok 4.6 `grok-4.6`):**
  PR #179's hook now **blocks** `behind` / `diverged` / `remote-advanced`
  (including `--force`) before objects are sent; fetch retried at most 5
  times then fails closed; CRLF-in-blob stays a hard block; line-level
  silent-revert stays WARN-by-default with H2656 banner honesty. 10/10
  `tests/test_pre_push_stale_base.py`. Operator notes:
  `docs/PRE_PUSH_STALE_BASE_OPERATOR.md`.

- **H2297 BVP pagination pilot v2 (06-08-2026, Sonnet 5 `claude-sonnet-5`) —
  H1892 redo after FINDINGS §314 data loss:** H1892's 30-07-2026 pilot output
  is confirmed gone (destroyed when its worktree was removed before escrow);
  this run redoes it and, this time, escrows the output before any teardown.
  Found en route: `bvp/pagination.py`/`bvp/paginate_live.py`/
  `tests/test_bvp_pagination.py` were built and tested during H1892 but only
  ever landed on the throwaway branch `origin/h1892-bvp-pagination-pilot` —
  never a PR, never merged to `main`. Recovered verbatim (no
  re-implementation) via
  [PR #174](https://github.com/gasyoun/IndologyScholars/pull/174).
  Fresh live pilot (run directly in this persistent main checkout, not a
  throwaway worktree): 3/3 pages, 90/90 unique conversation IDs, 0
  duplicates, 0 faults, `displayed_total=23476`, `coverage_status=partial` —
  see `analytics_output/community_lenses/reports/bvp_source_assessment.md`
  § "Three-page pagination pilot v2" for the full table (compared against
  H1892's now-destroyed 23,468/90/0/0). Escrow: raw pages +
  `bvp/data/meta/pagination.json` stay gitignored inside this persistent
  checkout; the small hash/pointer record
  `bvp/data/meta/manifest_pin.json` is force-added and committed so a fresh
  `origin/main` worktree can verify reachability without re-scraping —
  smoke-tested per Procedure step 5, see the H2297 handoff completion
  report. `pytest tests/test_bvp_scrape.py tests/test_bvp_pagination.py`:
  12 passed. Comment posted on
  [IndologyScholars#169](https://github.com/gasyoun/IndologyScholars/issues/169)
  linking this recovery. H1896's own dependency section can now find both
  the H2242 `LensAdapter` contract and this pinned manifest.

- **H1896 BVP Wave 1C adapter (2026-07-31, Sonnet 5 `claude-sonnet-5`):**
  `community_lenses/adapters/bvp.py` — pure reader over the frozen,
  gitignored `bvp/data/meta/state.json` + `bvp/data/parsed/*.json` acquired
  by PR #153's `bvp/scrape.py`. Pinned manifest
  `bvp:partial:2026-07-29:386f655244ed` (state.json SHA-256, first 12 hex).
  Coverage: 30/23,467 listed conversations enumerated (0.1278%); 156 native
  messages, 155 included + 1 excluded (semantic-DOM fallback record with no
  public author/body, kept explicit, never dropped); 0 permanent failures.
  `coverage_status` stays `partial`; `supports_population_metrics=False`
  gate (`require_population_metrics`) mechanically blocks any
  trend/person-share/topic-share claim until a separate pagination unit
  reconciles the full listing. Report:
  `analytics_output/community_lenses/reports/bvp_adapter_reconciliation.md`.
  This session found the files already present, uncommitted, from an
  unfinished prior run (mtimes ~05:46 local, predating this session, with no
  matching `.ai_state.md`/report-delivery trail) — independently
  re-verified rather than trusted: `pytest tests/test_bvp_adapter.py
  tests/test_community_lenses_schema.py tests/test_community_lenses_adapters.py`
  76 passed, `py_compile` clean, `git check-ignore bvp/data/raw
  bvp/data/meta/state.json` both ignored, `git diff --check` on all three
  owned files clean, `git status --short` shows no raw capture/attachment/
  secret/out-of-scope file added by this handoff. Local implementation only
  per handoff scope — not committed, not pushed.

- **H1895 Wave 1B adapters (2026-07-31, Sonnet 5 `claude-sonnet-5`):**
  `community_lenses/adapters/{conferences,nagari,vk_ors,indology_l}.py`
  (BVP is H1896's territory, untouched). Each exposes `build_fixture()` (real
  data, H1893-fixture-shaped) and `coverage_report()`; per-source reports at
  `analytics_output/community_lenses/reports/{conferences,nagari,vk_ors,
  indology_l}_coverage.md`. `tests/test_community_lenses_adapters.py` (37
  tests: idempotence, stable IDs, fail-closed validation, graceful-degrade,
  rights defaults, no shared_topic pre-emption). Per-corpus outcome:
  - **conferences** — real, `complete`: reads `conferences.db` (40 events,
    243 sessions, 1362 presentations, 268 speakers matching the published
    speaker count). Reuses `analytics_output/gumilyov_scale.csv`
    (argument_level G1-G3), `analytics_output/renou_presentation_matches.csv`
    (renou_state/renou_register), event `theme_ru`/`theme_en` (native_topic),
    and `authority_ids.json` confidence (`confirmed`/`manual` → person
    `review_status='accepted'`) verbatim — no new classification, no
    crosswalk. `theme_codes_final_v2.csv`'s l1/l2/l3/l4 codes are
    deliberately NOT mapped into `shared_topic` (H1897's scope).
  - **vk_ors** — real, `complete`: reads the already-built
    `vk-ors/data/vk_ors.db` (7,608 posts, 2,052 real per-post hashtags via
    that package's own `RE_HASHTAG` extraction). Only real hashtags feed
    `native_topic`; no keyword-derived topic is substituted (only aggregate
    per-corpus term counts exist for VK, not per-post). One broadcast
    account — no fabricated participant identities.
  - **nagari** — `pilot` (or `unavailable` if no `nagari.db` exists on the
    running machine): the raw 1.88 GB Takeout mbox exists only in the main
    tree (`IndologyScholars/nagari-2005-2026/`, gitignored, absent from this
    worktree), so this session ran a bounded 300-message pilot slice via the
    existing `nagari_group_archive.ingest.build()` (never reimplemented) to
    produce a local, gitignored `nagari/data/nagari.db`. Reuses the group's
    own native two-level taxonomy (`nagari_group_archive.taxonomy.classify`,
    registered as its own non-shared `nagari_native_taxonomy` scheme, not
    crosswalked) and redaction defaults (`redact.mask_name` on
    `from_email`). Never resolves a message author to a `person` row
    (H1898's scope). Full (non-`--limit`) ingest was deliberately NOT run
    here — that is a several-GB, long-running first-time operation outside
    this handoff's bounded scope; a future session can re-run
    `nagari_group_archive.ingest.build()` without `--limit` for full coverage.
  - **indology_l** — deliberately `unavailable`: H1894 (atomic feed
    hardening) was never actually built (see Dev Notes) despite the registry
    saying Done, so this adapter refuses to consume the old unhardened
    `tools/fetch_indology_feed.py` output and reports a named gap instead of
    fabricating a snapshot, per H1895 requirement 5 (reject mixed/partial
    input).
  - Inputs now available for **H1897** (crosswalk) and **H1898** (identity/
    quotes): conferences/nagari/vk_ors adapter output (fixture-shaped, in
    `community_lenses.adapters.{conferences,nagari,vk_ors}.build_fixture()`)
    and the coverage reports above. indology_l stays blocked until H1894
    actually lands.
  - Local implementation only per handoff scope (no commit/push/PR/
    deployment). Full verification matrix green: `pytest` (264 passed),
    `unittest discover -s tests` (36 passed), `validate_publication.py`,
    `nagari/scripts/audit_publish_surface.py`, `git diff --check`.

- **BVP public acquisition unit (PR #153, 30-07-2026, Grok 4.5):** ruling 25
  replaced the impossible owner-export prerequisite with a bounded public
  scrape. Landed `bvp/scrape.py` + `bvp/fetch_hardening.py` + README, gitignore
  for `bvp/data/`, `tests/test_bvp_scrape.py`, and
  `analytics_output/community_lenses/reports/bvp_source_assessment.md`. Live
  first-page slice: HTTP 200; 23,467 displayed conversations; 30 IDs
  discovered/fetched/parsed; 156 native message IDs (one incomplete public
  record); 2 transient listing retries; 0 permanent failures. Coverage remains
  `partial` until pagination reconciles. Raw captures stay gitignored.

- **H1893 community-lenses shared schema + codebook contracts (2026-07-30,
  Sonnet 5 `claude-sonnet-5`):** `community_lenses/` package — `schema.py`
  (16-table SQLite DDL: corpus/source_snapshot/container/record/record_name/
  record_relation/person/person_name/person_match_assertion/taxonomy_scheme/
  classification_assignment/taxonomy_crosswalk/provenance_assertion/
  correction/annotation/quote, all FK/CHECK-enforced), `ids.py` (namespaced
  `record_id`, RFC Message-ID fallback hashing), `manifests.py`
  (`SourceManifest` contract + validators), `taxonomy.py` + six versioned
  codebook-contract CSVs (`native_topic`, `shared_topic`, `community_function`,
  `renou_state`, `renou_register` — reusing `generate_renou_layer.py`'s
  `RULE_ROWS` vocabulary — and `argument_level`), `build.py` (deterministic
  fixture loader/validator/round-trip), five representative fixtures (one per
  lens, BVP as an explicit `unavailable` placeholder), and
  `tests/test_community_lenses_{schema,codebooks,fixtures}.py` (47 tests).
  No adapters, no substantive crosswalk mappings, no identity/quote logic —
  those are H1894/H1895 (see Next Steps). ACL Anthology adopted/rejected
  ledger: `docs/ACL_STANDARDS_ADAPTATION_IndologyScholars_community-lenses.md`.
  Local implementation only per handoff scope; verification matrix
  (`pytest`, `unittest discover`, `validate_publication.py`) green.

- **H1529 docs micro-residual (2026-07-23, Grok 4.5):** `.ai_state` release line
  and `docs/ROADMAP_2026.md` Phase 4 deposit wording; restore `CITATION.cff` after
  post-merge rebuild clobber. Release **v1.4.2**.

- **H1528 stale documentation refresh (2026-07-23, Grok 4.5):** living docs
  realigned to post-v1.4.0 reality — README/README_EN (nagari, vk-ors,
  historical layer, IndologyArchiveAtlas spin-out), `.ai_state` header/queue,
  ROADMAP + ROADMAP_2026 Phase 3 ticks (H1467/H1072), `missing_birth_years.md`
  BIOGRAPHICAL_DATA recipe, docs index + development guides + CLAUDE.md +
  nagari sibling link. Release **v1.4.1**.

- **H1443 Vigasin corpus landed as raw sources (2026-07-22, Sonnet 5 `claude-sonnet-5`):**
  27 full-text `.mdx` conversions of A.A. Vigasin's *Изучение Индии в России* landed under
  `sources/vigasin/` — [PR #124](https://github.com/gasyoun/IndologyScholars/pull/124),
  merged. Routed by scholar where a `/s/<slug>` registry row already exists in
  `curation/historical_persons.csv` (`minaev-ivan`, `oldenburg-sergei`, `shcherbatskoi-fedor`,
  `stal-fon-golshtein-aleksandr`, `mervart-aleksandr`); 4 of 8 target scholars have **no
  registry row at all** (Rosenberg, Mironov, Pallas, Lyudmila Mervart) — landed under
  `scholars/_unregistered/<name>/`, adding their `historical_persons.csv` rows is a follow-up.
  This is raw material, not a page update: the site's `/s/<slug>` pages are fully generated
  static HTML with no MDX pipeline and no per-scholar content directory —
  `generate_scholars_pages.py`'s `build_memorial_html()` explicitly documents Phase 3
  (memorial essay) and Phase 4 (bibliography) as unbuilt placeholders; wiring these sources
  into the generator is a separate future handoff. Published as full text, public repo, risk
  explicitly accepted by the repo owner 22-07-2026 (no prior rights review existed for this
  corpus).
- **H1376 A25: single-snapshot reconciliation of the submitted manuscript
  (21-07-2026, Fable 5 `claude-fable-5`):** `article/ppv_submission_article.md`
  carried two irreconcilable corpus snapshots at once (A: 1362/890+472/1388,
  G1 1146; B: 1352/880+472/1378, G1 1161). Regenerated every table and prose
  figure from the frozen `article/snapshots/2026-07-17` tree (now named in §2
  as the reproducibility source); the G3→G2 editorial reclassification is
  stated once in §5 and propagated (G1 1146 / G2 207 / G3 9 / 1362); null-model
  expectation corrected to 131 (z −23); city-only share to 70.3% (also
  dissolves the A25↔A26 drift); §5 meso counts to 217/115/80/69/69/66; retention
  by city marker to 31.7/63.1/64.6. `check_ppv_numbers.py` extended to cover
  its blind spots (§4 summary row, §7 conclusion, affiliation denominator,
  discipline/period column sums, meso block, null-model + city-only figures,
  verified editorial-reclassification registry) — proven red on the pre-fix
  text (25 drifts, exit 1), green after (0, exit 0). Gate: validate_publication
  OK, pytest 161 passed, 35,456 chars ≤ 40,000; `ppv_draft.docx` rebuilt.
- **H459 Renou rule table: anchor the Cyrillic stems (19-07-2026, Sonnet 5
  `claude-sonnet-5`):** Fixed the measured 7.1% mid-word-Cyrillic-substring
  defect in `generate_renou_layer.py`'s `RULE_ROWS` (`docs/renou-precision-audit.md`
  §2) — every bare Cyrillic alternative now carries a `(?<![а-яё])` left
  lookbehind, and the two worst offenders (`тика`, `ману`) additionally carry
  a `(?![а-яё])` right lookahead since they are complete transliterations, not
  Russian morphological prefixes. «Эротика в Ригведе» (Ṛgveda paper) no longer
  misfiles as `register:bhasya` via «тика»; «Рамануджа» no longer fires
  `register:smrti` via «ману». Acceptance test (same predicate that measured
  the floor): 121/1,706 rows failed before, 0/1,559 after. Also exported the
  missing `matched_field`/`matched_field_text` columns on
  `renou_presentation_matches.csv` (scope item 2 — 14% of the precision gold
  sample matched an un-exported subject tag). Found and fixed a 4th, previously
  unnoted duplicate of the rule table: `curation/renou_conference_rules.csv`
  is seeded from `RULE_ROWS` once and never again (`seed_rules()` only writes
  if absent) — it had gone stale since PR #66, so editing `RULE_ROWS` alone
  would have had **zero** effect on the real pipeline output; deleted it so it
  reseeds from the fixed source. Archive layer (`Indology/data/curation/renou_subject_rules.csv`)
  carries no Cyrillic alternatives at all and is unaffected — re-ran it anyway
  per scope item 4, coverage byte-identical (10.0%/13.8%). Added 10 regression
  tests (`tests/test_renou_layer.py`); `python -m pytest` 161/161 green;
  `validate_publication.py` green. **Not done, on purpose:** scope item 3
  (deduplicate the rule table into one canonical file across
  `generate_renou_layer.py` / `Indology/data/curation/renou_subject_rules.csv`
  / `SanskritLexicography/RussianTranslation`) stays open — gated on the
  handoff's own `@DECIDE` (canonical home: `RussianTranslation` next to
  `RENOU.md`, or `sanskrit-util`) which this session cannot resolve. Scope
  item 5 (raise coverage) correctly not attempted — gated behind item 3 by the
  handoff's own ordering. Before/after tables recorded in
  `docs/renou-precision-audit.md` §5. PR: (opened this session, see repo PRs).

- **H921 health-axis reconcile (2026-07-14, Sonnet 5 `claude-sonnet-5`):** main-tree checkout
  was stranded on `readme-refresh-h550` (its 2 commits already merged via
  [PR #85](https://github.com/gasyoun/IndologyScholars/pull/85), branch stale/24 behind
  `main`), with an untracked `nagari-2005-2026/` (1.8GB Google Groups mbox dump) that turned
  out to already be correctly gitignored on `main` (a known, rights-gated closed-mailing-list
  archive — my own redundant `.gitignore` edit was reverted, not landed) plus an untracked
  `.githooks/pre-commit` byte-identical to `origin/main`'s tracked copy (stashed, not deleted,
  to unblock checkout). Checked out `main`, fast-forwarded 8cd3eeedb→7de400187. Tree clean,
  `main` == `origin/main`, `core.hooksPath` confirmed. Reconciled
  [PR #97](https://github.com/gasyoun/IndologyScholars/pull/97) (green mechanical link-audit
  fix, unrelated pre-existing PR) — merge deferred to MG per session judgment call, not
  auto-merged.

- **H718 link/anchor integrity + FAIR metadata pass (2026-07-12, Fable 5 `claude-fable-5`):**
  Audited 2,504 published pages / 196,786 internal links / 4 ID registries /
  2 datapackages with the new read-only `tools/audit_archive_integrity.py`;
  report in `docs/link-integrity-fair-audit-07.2026.md`. Fixed: stale orphan
  `gumilyov/level-0.html` (14 broken links, self-perpetuating sitemap row;
  generator got a stale-level cleanup), 5 dangling `slug_redirects.json` rows
  remapped to live merged-into persons, `hypotheses.html` hash-restore for 33
  dynamic `#H##` deep links (verified: `#H16` lands at viewport top), Indology
  `CITATION.cff` version+ORCID added and wrong-org repository URL
  (sanskrit-lexicon→gasyoun) fixed via `public_metadata.py` + regen. Corpus
  counts untouched; validate green, pytest 147/147. Residual informational:
  27 by-design legacy-retained public ids. New @DECIDE: url_slug `vladimir`
  for Шелкович (rename vs URL persistence).

- **H674 — A26 data paper verify + complete, no DOI (2026-07-11, Fable 5 `claude-fable-5`, [PR #90](https://github.com/gasyoun/IndologyScholars/pull/90) merged):**
  Every derivable figure in `article/data_paper_draft.md` re-verified against the
  committed data. Confirmed: 1,362/268/1,388 corpus counts, 40 events / 22 years,
  87.3% birth coverage, Zograf city-only 70.3% (appendix G H4). Fixed stale: schema
  10→19 tables (+`person_kind`/26 historical figures), OpenAlex 122→181 candidates
  (496 rows, all `todo`), identifier-coverage table refreshed (Wikidata 3 / ORCID 7 /
  OpenAlex 14 of 268, candidate-vs-verified split), edge types 5→6
  (`organization_theme`), CSV exports 40+→100+, target journal JOHD→**Research Data
  Journal (Brill)** per the locked decision. New sections: §5.3 dual licensing
  (Apache-2.0 code / CC BY 4.0 data), §5.4 data availability (deposit NOT performed,
  re-freeze at deposition), §6.4 cross-model κ figures (L1 0.670, level 0.553) with
  human-IRR caveat, §6.7 name-heuristic FP limitation (the -вич mis-parse of PR #84
  as the worked example). Gate hardened: `check_data_paper_numbers.py` 10→19
  machine-verified claims (city-only vs appendix G, OpenAlex vs CSV, tables vs DB,
  authority rows vs JSON). `article/zenodo_metadata.json` pre-staged, clearly marked
  NOT YET DEPOSITED (org Zenodo freeze until after 15-07-2026; DOI needs explicit
  permission). Verification: gate green (1 honest κ warning), validate_publication
  green, pytest 147/147. Remaining before submission: human IRR pass, Zenodo deposit
  + DOI, re-freeze snapshot.

- **H484 Фаза 2 — исторический просопографический слой (2026-07-10, Opus 4.8 `claude-opus-4-8`):**
  Решение **A1 принято человеком — вариант A**: один спайн `person` +
  дискриминатор `person_kind` (`conference_participant` | `historical`). Загружен
  первый транш **26 исторических фигур** (Лебедев … Серебряков) как источник истины
  в `curation/historical_persons.csv` (+ `historical_person_roles.csv`), сидер
  `pipeline/historical.py`. Резолвер `tools/resolve_historical_wikidata.py` (реюз
  `scratch/scrape_common.py` + `wikidata_enrich._year`) взял даты/Q-ID/институции прямо
  из Wikidata. Итог в БД: 268 участников + 26 исторических = 294 персоны; **счётчик
  докладчиков остаётся 268** (все публикуемые счёты идут через `presentation_person`);
  78 сорсед `data_assertion` (curator `h484_historical_seeder`) + **исходные 803
  сохранены**; 82 `person_role` (было 2). 17 дореволюционных строк **перенесены, а не
  продублированы** — `indologists.html` их больше не показывает; попутно исправлен Q-ID
  Антоновой (`Q12486524`→`Q19933264`). Мемориальные страницы 26 фигур рендерятся
  (`historical_scholars` в `site_data.json`). `validate_publication.py` чист, pytest
  124/124 (новый `tests/test_historical_layer.py`). **NB:** лог сборки печатает
  `curated data_assertion rows preserved: 881` (803 исходных + 78 новых), а не 803 —
  ожидаемо, инвариант «исходные 803 не тронуты» держится. **Сеть:** Wikidata REST и
  ru.wikipedia оказались доступны с этого хоста 10-07-2026 — вопреки записям WIP ниже.

- **INDOLOGY author search facet fix (2026-07-01):**
  Fixed the static archive search so author results can be filtered by any
  topic/list-function activity, not only each author's top topic/function.
  `search_authors.json` now carries `topic_counts` and
  `list_function_counts`; browse facet clicks preserve Authors mode when the
  current view is authors. Verification: regenerated search/validation, local
  JS simulation returns Madhav Deshpande for `Grammar and linguistics`, and
  search validation now checks author facet fields.

- **Renou cross-site comparison layer (2026-07-01):**
  Added a static comparison page and CSVs linking the main conference Renou
  layer with the standalone INDOLOGY archive Renou outputs. The comparison keeps
  presentation counts separate from archive message/thread counts and labels the
  result as contrastive metadata, not rank or representativeness. Verification:
  regenerated Renou outputs, checked comparison-page links, ran publication
  validation and Python compilation.

- **Main-site Renou conference layer (2026-07-01):**
  Added a separate Renou state/register finding aid for the main
  `IndologyScholars` conference metadata, parallel to but separate from the
  INDOLOGY archive layer. Outputs include `findings/renou.html`,
  `curation/renou_conference_rules.csv`, presentation/match/summary/export CSVs
  under `analytics_output/`, datapackage/download/data-dictionary links, and
  deploy/validation workflow hooks. Verification: `generate_renou_layer.py`,
  `validate_publication.py`, py_compile, Renou page link check, and
  `prepare_pages_artifact.py` all passed locally.

- **INDOLOGY monthly mbox parser follow-up (2026-07-01):**
  Second manual dispatch of the monthly updater reached the full harvest and
  exposed a malformed historical `Message-ID` header that Python 3.11's
  structured email policy rejected. Switched mbox header parsing to the tolerant
  stdlib `policy.compat32` mode so archival headers are preserved as raw text
  instead of aborting the harvest.

- **INDOLOGY monthly CI follow-up (2026-07-01):**
  First manual dispatch of the new monthly updater exposed a Python 3.11
  incompatibility in `Indology/indology_archive_research/public_metadata.py`
  (`f-string expression part cannot include a backslash`). Fixed by precomputing
  the escaped data-dictionary description outside the f-string expression.

- **INDOLOGY monthly live update automation (2026-07-01):**
  Added a dedicated GitHub Actions workflow for the standalone
  `IndologyArchive/` atlas. `.github/workflows/update_indology_archive.yml`
  runs on the 1st of each month at 03:00 UTC, can be started manually,
  regenerates `Indology/` through the cache-aware Pipermail pipeline, refreshes
  the top-level archive index to discover new months, commits changed atlas
  files, prepares the public `IndologyArchive/` Pages artifact, and redeploys
  GitHub Pages without rebuilding the main conference archive.

- **INDOLOGY Renou state/register layer (2026-07-01):**
  Added a sparse Renou subject-line crosswalk for the standalone
  `Indology/` atlas, based on
  `SanskritLexicography/RussianTranslation/RENOU.md`. New generated outputs:
  `data/curation/renou_subject_rules.csv`,
  `data/processed/renou_messages.csv`,
  `renou_message_matches.csv`, `renou_thread_matches.csv`,
  `renou_state_summary.csv`, `renou_register_summary.csv`, and
  `renou_coverage.csv`. Coverage is deliberately sparse: 6,217/62,112
  messages and 3,309/24,033 threads match at least one Renou state/register
  rule. Dashboard, report, data dictionary, datapackage, guardrails, manifest,
  and validation now expose the layer with the caveat that blank fields mean
  "not classified by this layer," not "not relevant to Renou."

- **INDOLOGY Archive offline/PWA fix (2026-06-30):**
  Prepared a service-worker refresh so the newly published
  `/IndologyScholars/IndologyArchive/` atlas is part of the lightweight core
  offline cache. The PWA cache version is bumped, key atlas entry/search/
  curation pages are pre-cached, IndologyArchive navigations fall back to the
  atlas landing page instead of the generic Russian offline screen, and
  `offline.html` now exposes a direct INDOLOGY-L Atlas link. Follow-up:
  slashless `/IndologyScholars/IndologyArchive` is explicitly normalized in
  both the service worker and generated `404.html`, so stale parent fallback
  pages should redirect into the atlas instead of behaving like a main-site 404.

- **INDOLOGY Archive discoverability follow-up (2026-06-30):**
  Added generator-level links so the live `IndologyArchive/` atlas is not only
  deployed but discoverable from the main portal. `generate_publication_pages.py`
  now includes the atlas in the static search index, the English homepage card
  grid, and `sitemap_static.xml` entries for the landing, dashboard, search,
  and curated pages.

- **INDOLOGY Archive Pages publish fix (2026-06-30):**
  Prepared follow-up deploy fix for the merged `Indology/` atlas. The Pages
  artifact builder now publishes source `Indology/` under public path
  `IndologyArchive/` and writes `IndologyArchive/index.html` as a redirect to
  `dashboard/index.html`, giving the intended URL
  `/IndologyScholars/IndologyArchive`. Also synchronized stale public count
  snippets to the rebuilt baseline (268 speaker profiles; 163 Zograf-only) and
  refreshed `conferences.db` so the CI ID-stability audit starts from the
  current stable-key baseline. Verification in a clean worktree:
  `python validate_publication.py`, `python article/check_ppv_numbers.py`,
  `python article/check_anonymity.py`, `python article/check_data_paper_numbers.py`,
  and `python prepare_pages_artifact.py`; `_site/IndologyArchive/index.html`,
  `_site/IndologyArchive/dashboard/index.html`, and
  `_site/IndologyArchive/data/processed/messages_clean.csv` were present.

- **INDOLOGY semver tracking start (2026-06-30):**
  Added initial semantic-version anchors for the standalone `Indology/`
  appendix: `VERSION` = `0.1.0`, `CHANGELOG.md` with the first release entry,
  and `indology_archive_research.__version__ = "0.1.0"`. README now states the
  current standalone appendix version and where to update version history.

- **INDOLOGY publication maturity layer (2026-06-30):**
  Added standalone public-data metadata for `Indology/` without merging it into
  the main `IndologyScholars` generated site. New module
  `Indology/indology_archive_research/public_metadata.py` writes
  `data_dictionary.md`, `datapackage.json`, `CITATION.cff`,
  `data/processed/human_review_index.csv`,
  `data/processed/human_review_summary.json`,
  `data/processed/interpretive_guardrails.csv`, and
  `reports/interpretive_guardrails.md`. `scripts/run_pipeline.py` now runs the
  layer after search/publication and before validation, with
  `--skip-public-metadata`. Publication outputs link the new documentation,
  review index, citation metadata, and guardrails; validation now checks
  datapackage JSON/path resolution, CFF basics, data-dictionary family coverage,
  review-index domains, and major overclaim warnings. Verification:
  regenerated publication -> public metadata -> publication -> public metadata
  -> validation from cached data; `datapackage.json` lists 55 existing
  resources with 0 missing paths; `human_review_index.csv` has 1,921 rows
  across author normalization, case-study review, first-review shortlist,
  count mismatch, noisy subject, and reply-network domains; validation reported
  no blocking sections; `python -m compileall indology_archive_research scripts`
  passed.

- **INDOLOGY review intake round-trip (2026-06-30):**
  Added conservative human-review import workflow for case-study curation.
  New module `Indology/indology_archive_research/review_import.py` seeds
  `data/curation/first_review_notes.csv` from the 25-row shortlist and imports
  filled notes into `data/processed/curated_case_studies.csv`, with dry-run,
  `--force`, validation of status/track values, non-overwrite default behavior,
  and `review_import_audit.csv`. `scripts/run_pipeline.py` now exposes optional
  `--seed-review-notes`, `--import-review-notes`, `--review-import-dry-run`,
  and `--review-import-force`. Public outputs link the editable intake and audit;
  validation now checks selected cases for `public_note`, `why_it_matters`, and
  valid local/archive links. Verification: seeded 25 review-note rows; dry-run
  left curated data unchanged; normal sample import updated only allowed/empty
  fields and promoted default `candidate` to `selected`; `--force` overwrote
  non-empty test fields; invalid status/track values were recorded as audit
  errors; temporary test inputs were removed and sample row restored; regenerated
  `curation`, `search`, `publication`, `validate`; final clean state has 25
  notes rows, 25 audit rows (`unchanged`), 0 selected rows, no TEST residue, and
  `python -m compileall indology_archive_research scripts` passed.

- **INDOLOGY first-review worksheet (2026-06-30):**
  Added `reports/first_review_worksheet.md`, generated from
  `first_review_shortlist.csv`, with one English-only mini-review form for each
  of the 25 shortlist threads. Each section includes metadata, local/public
  links, generated selection evidence, and standard prompts: what the thread is
  about, why it is useful for the atlas, visible caveats, recommended public
  note, and follow-up checks. `dashboard/curated.html`, `dashboard/index.html`,
  `README.md`, and `reports/validation.md` now link/validate the worksheet.
  Verification: regenerated `curation`, `search`, `publication`, `validate`;
  confirmed 25 worksheet sections for 25 shortlist rows, standard prompts
  present, worksheet links present in dashboard/curated pages, and `python -m
  compileall indology_archive_research scripts` passed.

- **INDOLOGY balanced first-review shortlist (2026-06-30):**
  Added `first_review_shortlist.csv` as a deterministic 25-thread starting set
  for human case-study review. It balances high overall score (7),
  philological-substance track (5), infrastructure-history track (5),
  debate/controversy (3), community memory/announcement (3), and
  high-participation unassigned candidates (2), with
  `review_priority_reason` recorded for every row. `dashboard/curated.html`,
  `dashboard/index.html`, `reports/research_report.md`, `reports/validation.md`,
  `README.md`, and `dataset_manifest` now link/describe the shortlist as a
  review aid rather than a ranking. Verification: regenerated `curation`,
  `search`, `publication`, `validate`; confirmed 25/25 rows are in
  `case_review_queue.csv`, 25/25 have local thread pages or public archive
  fallbacks, all have priority reasons, and `python -m compileall
  indology_archive_research scripts` passed.

- **INDOLOGY reviewer packet exports (2026-06-30):**
  Added split, regenerated review packets to make the 250-thread curation queue
  easier to work through in batches:
  `case_review_queue_philological.csv` (37 rows),
  `case_review_queue_infrastructure.csv` (16 rows), and
  `case_review_queue_unassigned.csv` (197 rows). `dashboard/curated.html`,
  `dashboard/index.html`, `reports/research_report.md`, `reports/validation.md`,
  `README.md`, and `dataset_manifest` now link/describe the packets. Validation
  checks that packet row counts add back up to `case_review_queue.csv`
  (250/250). Verification: regenerated `curation`, `search`, `publication`,
  `validate`; confirmed packet links in curated/dashboard pages and manifest;
  `python -m compileall indology_archive_research scripts` passed.

- **INDOLOGY curation/network clarity pass (2026-06-30):**
  Improved the generated curation and named-network layer after audit.
  `case_review_queue.csv`, `curated_case_studies.csv`, `dashboard/curated.html`,
  `dashboard/search.html`, and validation now expose `suggested_track_basis`
  for philological-substance / infrastructure-history hints. Current queue:
  250 rows; 37 philological-substance suggestions, 16 infrastructure-history
  suggestions, 197 deliberately unassigned pending human review. Added
  `is_self_reply` to `named_reply_network_summary.csv`; public report/dashboard
  samples now omit self-reply rows while keeping them in the downloadable data.
  Verification: regenerated `curation`, `search`, `publication`, `validate`;
  confirmed search rows include track basis, curated page exposes the basis
  column, named reply summary has `is_self_reply` (1,717 self-reply rows,
  22,827 inter-author rows), and `python -m compileall indology_archive_research
  scripts` passed.

- **INDOLOGY curated cases + named network summaries (2026-06-30):**
  `Indology/` now has an English-only curation layer via
  `indology_archive_research/curation.py`. Outputs:
  `case_review_queue.csv` (250 rows), extended `curated_case_studies.csv`
  with stable status/track/human-note fields, `curated_case_summary.csv`,
  `named_reply_network_summary.csv` (24,544 `direct_reply` rows), and
  `named_coparticipation_network_summary.csv` (47,102 `co_participation`
  rows). Added `dashboard/curated.html` with selected/candidate/rejected
  sections and separate philological-substance vs infrastructure-history
  tracks. `dashboard/index.html`, `dashboard/search.html`,
  `reports/research_report.md`, `reports/validation.md`, `README.md`, and
  `dataset_manifest` now expose the curated workflow and named network tables.
  Verification: cached regeneration (`thread_explorer`, `curation`, `search`,
  `publication`, `validate`), 250/250 queue links valid, 0 invalid statuses or
  tracks, search status parity with `curated_case_studies.csv`, unchanged
  `messages_clean.csv` row count (62,112), and `python -m compileall
  indology_archive_research scripts` passed.

- **INDOLOGY static search and browse layer (2026-06-30):**
  `Indology/` now generates metadata-only static search indexes via
  `indology_archive_research/search.py`: `search_threads.json` (250 rows),
  `search_authors.json` (3,150), `search_topics.json` (12), and
  `search_messages_sample.json` (4,064). Added `dashboard/search.html` with
  plain HTML/JS search and browse controls for thread subjects, authors, topics,
  years, list functions, candidate types, compact message metadata, local
  thread pages, and public Pipermail links. `dashboard/index.html` links
  prominently to the search page; `dataset_manifest` now includes JSON outputs;
  `reports/validation.md` verifies JSON validity, local/fallback thread links,
  and curation-status parity with `curated_case_studies.csv`. Verification:
  staged regeneration (`search`, `publication`, `validate`), JSON/link/status
  checks, unchanged `messages_clean.csv` row count (62,112), and `python -m
  compileall indology_archive_research scripts` passed.

- **INDOLOGY thread explorer + case-study curation seed (2026-06-30):**
  `Indology/` now generates static local thread pages for the top 250
  `case_study_candidates.csv` rows via
  `indology_archive_research/thread_explorer.py`. Outputs:
  `thread_explorer_index.csv` (250 rows, all pages present, all archive URLs
  valid), `curated_case_studies.csv` (250 rows, all `candidate`), and 250 HTML
  pages under `Indology/dashboard/threads/`. Each page shows metadata-only
  chronology, participants, public Pipermail links, directed reply confidence,
  unresolved reply-like messages, and why the thread was selected. Dashboard now
  includes a Case Study Explorer with linked tables for long/request/debate/
  digital/memorial/philological candidate threads; report documents how to read
  thread pages. Verification: staged regeneration (`thread_explorer`,
  `publication`, `validate`), page/index/URL/curation/message-count checks, and
  `python -m compileall indology_archive_research scripts` passed.

- **INDOLOGY guided atlas appendix (2026-06-30):** new standalone subproject
  `Indology/` analyzes the public INDOLOGY Pipermail archive (1990-11..2026-06)
  as scholarly infrastructure. Current generated corpus: 62,112 messages,
  24,033 threads, 3,283 raw author strings, 42,741 directed reply rows. Added
  atlas layer (`Indology/indology_archive_research/atlas.py`) that writes
  `atlas_timeline.csv`, `atlas_topic_profiles.csv`, `atlas_list_functions.csv`,
  `atlas_people_summary.csv`, `atlas_reply_summary.csv`, and
  `case_study_candidates.csv` (250 data-driven thread candidates). Dashboard
  `Indology/dashboard/index.html` is now a static guided atlas organized around
  six questions: what changed over time, what was discussed, what work the list
  did, who participated, who replied to whom, and threads worth reading.
  Narrative report expanded to `INDOLOGY-L as Scholarly Infrastructure,
  1990-2026`. Verification: staged cached regeneration via cleaning,
  reply_network, atlas, publication, validate modules; all atlas CSVs nonempty;
  list-function coverage 62,112/62,112; candidate URLs valid; `python -m
  compileall indology_archive_research scripts` passed. Note: full
  `python scripts\run_pipeline.py --insecure` can exceed a 5-minute command
  timeout because it replays all cached month files; staged regeneration
  completes.

- **A25 (ППВ-статья) — ORCID + author-block sync (2026-06-28):** ORCID
  `0000-0003-4513-884X` (канон — `Uprava/AUTHOR.md`) проставлен во все живые
  артефакты A25 — `ppv_submission_article.html` (RU+EN, был плейсхолдер; PR #47)
  и отмечен ✅ в `ppv_submission_checklist.md`; в submitted `.md` и
  `ppv_cover_letter.md` он уже был. Затем (PR #48) HTML-рендер
  `ppv_submission_article.html` синхронизирован с submitted `.md` по блоку автора:
  ученая степень (кандидат филологических наук) + рабочий адрес (Обнинск, Усачева
  21-285, 249030) в RU+EN (был `____`/устаревшая «Москва»); строка-89 чеклиста
  очищена от уже-сделанного ORCID. Анонимная копия остаётся без ORCID (слепое
  рецензирование). E-mail НЕ менялся: `gasyoun@gmail.com` — адрес, под которым
  статья подана 2026-06-02 (исключение зафиксировано в `Uprava/AUTHOR.md`;
  канонический `gasyoun@ya.ru` — для новых/неподанных работ). Устаревшие черновики
  `ppv_draft.*` не трогали (superseded; правка дала бы CRLF-шум).
- **VIS_009 rebuild (2026-06-26):** `cohort_survival.csv` generation moved from
  `scratch/closedness_metrics.py` into `generate_analytics.py`; output now uses
  censoring-aware Kaplan-Meier fields (`at_risk`, monotone survival) matching the
  VIS_009/VIS_044 page semantics. Added `tests/test_closedness_metrics.py`.
  Verification: `python -m pytest tests\test_closedness_metrics.py`,
  `python generate_analytics.py`, `python generate_publication_pages.py`,
  `python validate_publication.py`, and `python -m pytest --basetemp=.pytest-tmp`
  (101 passed).

- **DeepSeek видео+классификация: caption-пайплайн + provider-agnostic prep + раннеры (2026-06-22):**
  Цель — потратить недельный безлимит DeepSeek на (1) видео-слой и (2)
  классификацию; язык RU+EN, агрессивная публикация с пометами `(?)`.
  *Caption track (без LLM, YouTube доступен):* инвентаризация 176 видео —
  158 с RU auto-captions, 0 manual, 8 без (`tools/caption_inventory.py` →
  `analytics_output/caption_inventory.csv`); выкачка 161 VTT
  (`tools/pull_captions.py` → `scratch/youtube_captions/` + manifest); парсинг
  с rolling-window дедупом (`tools/parse_captions.py` → `scratch/transcripts/`,
  161 транскрипт ≈ 2.06M токенов RU, `whisper_queue.csv` = 8). *Dataset prep
  (`tools/build_deepseek_inputs.py`):* `scratch/classification_input.csv` (1362
  + prior-метки), `scratch/program_talks.json` (каталог докладов с докладчиками
  из `site_data` scholars[].talks), `scratch/segmentation_inputs/` (163 бандла:
  63 align_single / 18 segment_session / 82 verify_match, каждый с указателем на
  транскрипт + кандидатами-докладами года/серии). *Раннеры* (см. Dev Notes) —
  импорты/конфиг/логика голосования провалидированы без сети; запуск — с
  clean-egress хоста (см. WIP + Next Steps). Корпус-чисел не трогали.
- **Phase-5 local refresh / no supported promotions (2026-06-20):**
  пройден preflight: Wikidata REST и ru.wikipedia недоступны с текущего хоста,
  institutional scrape дал 0 новых кандидатов, OpenAlex high-confidence для
  инъекции = 0. Пересобраны `scratch/non_participants.md` и
  `analytics_output/roster_participant_links.csv`; текстовые summary-доки
  синхронизированы с validator-числами 270 профилей / 165 only-Zograf.
  `validate_publication.py` зелёный, `pytest` 100/100. Для реального подъёма
  verified coverage нужен повтор Wikidata/ruwiki-части на .ru-хосте.
- **Scope decision: Семенцовские чтения вне корпуса (2026-06-20):**
  зафиксировано, что PPV-статья остаётся сравнением двух основных долгосрочных
  площадок (Зографские/Рериховские); Семенцовские чтения — новый и маргинальный
  контекст, не третий корпус. Добавлены `docs/sementsov-readings-context-note.md`,
  ссылка в `docs/README.md`, guardrails по будущей работе (аффилиации, публикационная
  конверсия, видео/цифровая доступность, устойчивость двухплощадочной модели) и
  короткое уточнение в §1 `article/ppv_submission_article.md`.
- **Автономный спринт, часть 2 (2026-06-12):** (1) `check_data_paper_numbers.py` вписан в оба CI-workflow (gate + py_compile); (2) `VIS_044_km_retention` — настоящий Каплан–Мейер с правым цензурированием (последние 2 программных года = цензура) по периодам дебюта, серверный SVG в основном пайплайне (NB: старый `VIS_009` питается из `scratch/closedness_metrics.py` — нарушение правила 5, без цензурирования; кандидат на замену/пересборку); (3) `wilson_interval` перенесён в `publication_helpers.py`, на `findings/gender.html` добавлена годовая таблица доли женщин с 95% ДИ Уилсона и предупреждением о перекрывающихся интервалах (мёртвый код `years_rows` удалён).
- **Автономный спринт укрепления (2026-06-12, вечер):** (1) guard в `validate_publication.py` — per-ID сверка `argument_level` между классификационным CSV и `site_data.json` + запрет талков без уровня; (2) `tests/test_gender_classification.py` (18 тестов) — выявил и исправил остаточный дефект: фамилии на -ович (Маркович) перебивали женское имя; порядок стадий теперь имя→отчество→фамилия; (3) LOD: темы L1 (с Wikidata sameAs из geography.json) и argument_level как DefinedTerm + schema:about на всех 1362 докладах; (4) `VIS_043_coding_reliability` — серверные confusion-матрицы слепой выборки на странице атласа + реестр в visualisations.md; (5) `article/check_data_paper_numbers.py` — гейт численных утверждений data paper (10 проверяемых сверены; 70.3% и 122 кандидата — warnings на ручную пересверку). validate+pytest 89/89 зелёные.
- **Научный аудит Brill-уровня (2026-06-12):** (1) единый `classify_gender` в `publication_helpers.py` (баги: «ярослав» в женском списке, мёртвый «ксен», подстрочный «ич»; дубль-классификатор в `generate_publication_pages.py` удалён, читает поле `gender`); категория U в `gender_stats` (M:102/F:151/U:17) + методическая заметка на `findings/gender.html`; `tools/validate_gender_inference.py` (выборка 60, Wilson CI). (2) Канонический `argument_level` во всех машинных выгрузках (`gumilyov_level` — legacy-алиас, mapping note в `data_dictionary.md`); `model_id` (резолвнутый снапшот API) логируется в классификации. (3) IRR переделан: слепой лист + отдельный ключ, стратификация (census G3=11, oversample G2=30), κ+bootstrap CI, α Криппендорфа, AC1 Гвета; кросс-модельная сверка Claude: L1 κ=0.670, G κ=0.553 (слабое место — граница G2/G3). (4) `docs/persons-data-policy.md`(+ru) — основание, исправление/возражение для живущих; дисклоужер same-model-аудита в data paper §2.4/§6. Build+validate+pytest 71/71 зелёные.
- **Сбор голосования (2026-06-10):** кнопка «Отправить преподавателю» в `voting.html` (Яндекс Форма, `VOTE_TEACHER_FORM_URL`) + `tools/merge_votes.py` → таблицы «кто как проголосовал». Build+validate зелёные, pytest 71/71.
- **Runbook Phase 5 + идемпотентность реестра (2026-06-10):** `docs/ru-enrichment-runbook.md` (таблица достижимости + 7 шагов); сидер дедуплицирует по имени, а не только по `registry_id` (год рождения в хеше → обогащение не плодит дубли); тест `test_no_duplicate_names_in_registry`. pytest 71/71.
- **Слияние ростера в корпус (2026-06-10):** реализован `docs/roster-merge-design.md` — `tools/build_non_participant_registry.py` → `curation/non_participant_indologists.csv` (94), `tools/link_roster_participants.py` → 100 линков + 2 Q-ID, `generate_registry_page()` → `indologists.html`, 10 тестов; full build + validate зелёные.
- **Фикс Wikidata-генератора (2026-06-10):** P106→Q18524037, P101→Q625510, удалён фиктивный Q126692818, источник как референс S854, дедуп работодателей, скип строковых P108; `inject_openalex_matches.py` — exit без requests + динамическая дата. Батч перегенерирован, pytest 60/60.
- **Дизайн слияния ростера (2026-06-10):** `docs/roster-merge-design.md`.
- **Трек интернационализации (2026-06-10):** OpenAlex-сопоставление (122 кандидата), инъектор авторитетов, Wikidata-батч + guide, англ. data paper, LOD-граф, inter-rater инструменты, замороженный снимок, notebook-пример, Q-ID городов/тем в geography.json.
- **Подпроект ростера индологов (2026-06-06, HEAD):** 197 индологов, en.wiki-мост, институц. скрапер, 60 тестов.
- **ППВ-спринт (2026-06-02):** ссылочный аппарат, аннотации, метаданные, обезличивание, pre-submission gate (validate + pytest + cover letter). Статья подана.
- **NEXT #7 Генеалогия:** граф в networks.html, city-to-institution audit.
- **NEXT #8 Даты / #10 VAK / #12 UX:** скраперы дат, VAK-парсер, UX-правки.

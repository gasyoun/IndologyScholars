# Project Objective: ППВ статья подана → углубление и расширение

Статус на 2026-06-02: **ППВ-спринт завершён.** Статья, обезличенная копия, DOCX,
иллюстрации и cover letter готовы к подаче. Фокус сместился на NEXT-трек ROADMAP.

## Текущий фокус (NEXT)

1. **Генеалогия и сети** — граф teacher-student в networks.html (20 edges, зелёный dashed),
   city-to-institution audit (719 меток, 22.9% matched).
2. **Покрытие данных** — скрапер дат рождения, VAK-парсер, баг соавторов исправлен.
3. **Сайт/UX** — tooltip clamp, карта кликабельна, горизонтальная вёрстка профилей.
4. **Модуль агентов** — план выноса (`SPINOUT_PLAN.md`), ждёт реализации оркестратора.

## Зафиксированные решения

1. **Цель:** углубление данных и визуализаций после подачи ППВ.
2. **Всё через генератор:** `site_data.json` и HTML/CSV/JSON — производные, править генераторы.
3. **Явная неопределённость:** `(?)` для открытых аффилиаций, `city-only` ≠ маргинальность.
4. **Git workflow:** stash → rebase → push → pop (CI авторебилдит, конфликтует с binaries).

## ➡️ Next Steps (Queue)

- Прогнать `tools/vak_parser.py` на актуальном Excel-файле ВАК
- Прогнать `tools/scrape_birth_years.py` — найти даты для 33 учёных
- #9: Публикационная конверсия (доклады → статьи/сборники)
- Страница ключевых слов + фильтрация мусорных
- Python-оркестратор для модуля агентов (Anthropic SDK)

## 🚧 Current WIP

- Документация обновляется (ROADMAP, CHANGELOG, .ai_state)

## 🧠 Dev Notes

- **Как генерируются страницы:** `generate_publication_pages.py` → `page_shell()` → `templates/base.html`.
- **CSS-токены:** `styles.css` использует `--text/--accent/--border/--muted/--soft/--panel`.
- **Генеалогия:** `teacher_student.csv` (22 строки) → `pipeline/genealogy.py` → `generate_site_data.py` → профили учёных. `known_relationships.csv` (32 строки) → `known-relationships.html`.
- **Сеть:** `network_data.json` содержит 20 genealogy edges типа `person_person_genealogy`, инжектируются через `generate_network_json.py`.
- **Сборка:** `build_and_populate_db.py` → `check_ppv_numbers.py` → `generate_site_data.py` → `validate_publication.py` → `pytest`.
- **Новые инструменты:** `tools/city_trajectory_audit.py`, `tools/vak_parser.py`, `tools/scrape_birth_years.py`, `tools/apply_birth_years.py`.

## ✅ Completed

- **ППВ-спринт (2026-06-02):** ссылочный аппарат, аннотации, метаданные, обезличивание, пересборка БД (0 drifts), pre-submission gate (validate + pytest 40/40 + cover letter).
- **#19 Баг соавторов:** Тавастшерна/Азаркина исключены через `presentation_person_exclusions.csv`.
- **#20 Генеалогические связи:** все 30+ уже в CSV (предыдущие коммиты).
- **#21 ППВ-правки:** caveat о программах, inline-ссылки на рисунки, расширенные подписи.
- **NEXT #7 Генеалогия:** граф в networks.html, city-to-institution audit.
- **NEXT #12 UX:** tooltip clamp, карта кликабельна, горизонтальная вёрстка.
- **NEXT #10 VAK:** парсер `tools/vak_parser.py`.
- **NEXT #11 Агенты:** `SPINOUT_PLAN.md`.
- **NEXT #8 Даты:** скрапер `tools/scrape_birth_years.py` + `tools/apply_birth_years.py`.

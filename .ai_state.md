# Project Objective: развитие sociology.html и gatekeeping.html под цель «научная защищаемость»

Сделать обе страницы строгими и переживающими рецензию (ППВ/IIJ): каждое
утверждение — с данными и оговорками; полемику гейткипинга свести к
проверяемым гипотезам. Язык — **русский, под научного рецензента ППВ**.

## Зафиксированные решения (от пользователя, не пересматривать без спроса)
1. **Цель:** научная защищаемость (не полемическая сила, не расширение охвата).
2. **Всё через генератор:** обе страницы строятся `generate_publication_pages.py`,
   таблицы/цифры — из БД и `analytics_output/`, каждое заявление — со ссылкой на CSV/фигуру.
3. **Конфликт интересов:** имена в кейсе 2026 (включая владельца архива М. Гасунса)
   **оставить**, но добавить явный disclosure-блок и переформулировать кейс как
   открытый вопрос, а не вывод. (Фаза 2.)
4. **Бездоказательный тезис о внешней активности «исключённых»:** оставить как явную
   *планируемую проверку* со ссылкой на ВАК-парсер (модуль philology-research-agents)
   **И написать готовое задание для DeepSeek** на верификацию. (Фаза 1.5.)
5. **Фигуры:** трогаем `scratch/generate_sociology_plots.py` (подписи/данные). (Фаза 1.)
6. **Порядок работы:** строго по фазам; показывать результат пользователю до перехода
   к следующему блоку.

## ➡️ Next Steps (Queue)
- **Фаза 4 — Интерактивный атлас визуализаций (findings/)**:
  - Разработка первой интерактивной визуализации `VIS_001_orbit_scatter` (двуязычный scatter-plot пересечения когорт, переходы на профили учёных).
  - Разработка второй интерактивной визуализации `VIS_002_affiliation_opacity` (временная шкала аффилиационной непрозрачности по годам и сериям).
  - Разработка третьей интерактивной визуализации `VIS_003_video_heatmap` (тепловая матрица покрытия YouTube-видео с учётом неопределённости `needs_review`).
  - Разработка четвертой интерактивной визуализации `VIS_004_keyword_alluvial` (потоковый аллювиальный граф Санкея для дрейфа ключевых слов и мезокодов).

## 🚧 Current Work-In-Progress (WIP)
- Нет активного WIP. Все задачи по Фазам 1-4 по социологии, гейткипингу и визуализациям, а также структурированию и верификации связей успешно завершены и закоммичены.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- **Как генерируются страницы:** функции `generate_sociology_page()` и
  `generate_gatekeeping_page()` в `generate_publication_pages.py` (вставлены перед
  `generate_classification_criteria_page`, вызываются в `main()` после неё). Общий стиль —
  константа `ARTICLE_STYLE` (extra_head). Обёртка — `page_shell(title, desc, canonical,
  body, structured_data, extra_head=...)` из `publication_helpers.py`; шаблон
  `templates/base.html` даёт nav+footer и грузит `assets/styles.css`.
- **Две системы CSS-токенов (важно!):** `styles.css` использует `--text/--accent/--border/
  --muted/--soft/--panel`; старый `index.css` использовал `--text-primary/--accent-primary/
  --border-color`. Генерируемые страницы грузят ТОЛЬКО `styles.css` — писать инлайн-CSS
  на токенах styles.css, иначе текст «исчезнет».
- **Данные под утверждения:**
  - таблицы поколений → `analytics_output/senior_absence_audit.csv` (когорты
    `absent_after_2022` и `absent_in_2026`; сорт = (-talks_before_threshold, display_name));
    генерится в `generate_analytics.py::generate_senior_absence_audit`.
  - сетевая дисциплина → `analytics_output/network_robustness_checks.csv` (колонки
    `forbidden_inference`, `required_sensitivity_check`, модели NET01..NETxx) — ЗОЛОТО
    для цели «защищаемость», брать формулировки оттуда.
  - граф → `network_nodes.csv` / `network_edges.csv` / `network_data.json`.
  - фигуры рисует `scratch/generate_sociology_plots.py`.
- **Сборка/проверка (обязательно перед коммитом):**
  `python generate_publication_pages.py` → `python validate_publication.py` →
  `python -m pytest`. Быстрая проверка только этих 2 функций без всего пайплайна:
  `python -c "import generate_publication_pages as g; g.generate_sociology_page(); g.generate_gatekeeping_page()"`.
- **Git-workflow:** репозиторий работает напрямую в `main`, CI авторебилдит и деплоит
  Pages. Полный билд обычно меняет немного производных файлов (в этой сессии — 7).
  Перед пушем: `git fetch origin main` → `git rebase origin/main` → `git push`.
  `findings/visualisations.html` даёт недетерминированный шум (переупорядочение ARC_DATA) —
  это свойство билда, не наша правка.
- Память Claude по проекту: см. ключи `visualisations-are-generated`,
  `indology-git-push-workflow`, `philology-research-agents-module`,
  `parser-slippage-and-id-churn`.

## ✅ Completed (Recent only)
- **Кураторское структурирование связей (закоммичено, 2026-06-01):**
  - Добавлен темпоральный столбец `temporal` во все структуры связей (`known_relationships.csv`, `generate_publication_pages.py`, `datapackage.json`, `data_dictionary.md`).
  - Все временные/статусные маркеры (`"сначала"`, `"затем"`, `"ранее"`) вынесены в `temporal`.
  - Упрощены и стандартизированы ярлыки связей (ученица, работал у).
  - Статусы обновлены до `confirmed` на основании прямого авторитета редактора с основанием `"Согласно редактору"`.
  - Таблица на [known-relationships.html](known-relationships.html) дополнена колонкой «Период / время».
- **Фазы 1, 1.5, 2, 3 (закоммичено, commit `2c8d4912`):**
  - Реализованы все запланированные правоки по научной защищаемости `gatekeeping.html` и `sociology.html`:
    - Переформулированы утверждения в терминах гипотезы институционального фильтра.
    - Введён кураторский слой внесетевых связей (`known_relationships.csv`, `known-relationships.html`).
    - Введена верификация выпускников Восточного факультета СПбГУ (`eastern_faculty_alumni.csv`, `extract_eastern_faculty_alumni.py`).
    - Разведены два механизма отсутствия (после 2022 года и в программе 2026 года).
    - Разработан и опубликован мета-документ редакционных решений на русском и английском языках (`docs/sociology-gatekeeping-editorial-decisions-ru.md`, `docs/sociology-gatekeeping-editorial-decisions.md`).
    - Запущены полноценные двуязычные английские версии страниц (`sociology-en.html`, `gatekeeping-en.html`).
    - Опубликована статичная страница listener talk marks (`voting.html`).
- **Фаза 0 (закоммичено, push `1929be35..0fa18712` в main, commit `0fa18712`):**
  обе страницы перенесены из рукописной статики в пайплайн; таблицы поколений теперь
  из `senior_absence_audit.csv` (рассинхрон устранён); инлайн-CSS переписан на токены
  `styles.css` и вынесен в `ARTICLE_STYLE`; внутренние ссылки → относительные; общий
  site-chrome + SEO/JSON-LD. Текст аргументации НЕ менялся.
  Проверки: build OK · validate passed · pytest 40 passed.

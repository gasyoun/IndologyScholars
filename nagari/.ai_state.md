# Project Objective: Searchable archive + 20-year retrospective of the closed Google Group «Общество ревнителей санскрита» (nagari@googlegroups.com, 2005–2026)

Turn the Google Takeout dump (1.88 GB `topics.mbox`, 18 729 messages, 2 333
members) into (a) a searchable SQLite+FTS5 DB with a Markdown mirror, (b) the
four Indology-atlas analysis layers exploiting the full bodies, and (c) the
self-contained HTML page «20 лет Обществу ревнителей санскрита: история
гуглгруппы». Built in worktree `IndologyScholars-nagari` (branch
`nagari-20yr-archive`) — main tree is a hard-block worktree repo.

## ➡️ Next Steps (Queue)
- [x] **H1104 — ✅ ЗАКРЫТ 17-07-2026, гейт зеленый.** Код: [PR #104](https://github.com/gasyoun/IndologyScholars/pull/104)
      (redact.py вынесен из page.py, применён в export_md.py; Sonnet 5 `claude-sonnet-5`,
      исполнитель хэндоффа — не минтинг-сессия, та лишь заполнила H1104 stub); артефакт
      независимо регенерирован и перепроверен параллельной сессией — зеркало (2 929
      файлов, 69,1 MB) и аудит `[md ] no third-party addresses found`
      ([PR #107](https://github.com/gasyoun/IndologyScholars/pull/107), Fable 5
      `claude-fable-5`, 17-07-2026); релиз v1.2.1 + CHANGELOG (Sonnet 5, PR #106).
      `md/` остается git-ignored: публикация — отдельный человеческий гейт ниже.
- [x] **MG `@DECIDE` RESOLVED 17-07-2026 (chat ruling, relayed by Sonnet 5
      `claude-sonnet-5`): publish the full redacted Markdown mirror.** Triggered by
      a site-usability complaint (`t_notable`/`t_books` rows and search results in
      `site/index.html` carried no link at all — no thread ID reached the page
      payload, so nothing was clickable). Given three options (publish mirror /
      in-page-only detail / metadata-only stub pages), MG chose full publish.
      `nagari/md/` un-ignored; `page.py` now threads `gm_thrid` → a
      `thread_md_url()` GitHub-blob link through `notable`, `threads` (search) and
      `book_top` (looked up by `gm_thrid`, not the sharing message's own subject —
      confirmed 106/367 `book_index.csv` rows have a subject/year that differs from
      their thread's starter message); `_template.py` wraps subject/filename cells
      in `<a>`. `scripts/audit_publish_surface.py` re-verified 0 leaked addresses
      across both `site/index.html` and the freshly-regenerated 2 929-file mirror
      before publish. Fills the pre-existing empty stub
      [H1143](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1143-Sonnet_IndologyScholars_nagari-md-mirror-publish_17.07.26.md).
- [x] **Follow-up 17-07-2026 (same session, Sonnet 5 `claude-sonnet-5`), MG-requested:
      each `md/` file now links back to the original Google Groups topic.**
      `export_md.py`'s `google_groups_url()` builds the documented
      `https://groups.google.com/d/msgid/nagari/<Message-Id>` deep link from the
      starter message's `Message-Id` (verified live: resolves straight to the
      real topic, no auth needed — consistent with H1143's "group is open, not
      closed"). Emitted as a `source_url:` front-matter field + a clickable line
      under the `# subject` heading. **Caught before publish:** naively embedding
      every `Message-Id` would itself leak PII the redaction step never touches —
      some mail servers build the Message-Id from the sender's own mailbox local
      part (`<user-domain-tld@...>` shape), re-identifying a masked name. Measured
      14/2 928 threads (0.5%) hit this; `google_groups_url()` compares the
      starter's own `from_email` against the Message-Id and omits the link for
      those 14 rather than dropping the whole feature. `audit_publish_surface.py`
      updated to skip `source_url:`/msgid lines (else ~2,900 false positives on
      opaque-but-email-shaped tokens); re-run clean, 0 leaks.
- [ ] MG `@DECIDE`: the 2 030 attachments (367 book-like, 294 PDF, 216 MB) have **no
      rights triage**. `ingest.py` stores metadata only and never extracts blobs —
      keep it that way until the rights question is ruled on. This is a rights call,
      not a privacy one.
      **Evidence base landed 17-07-2026 (H1142, Fable 5 `claude-fable-5`):** all 2 030
      blobs extracted locally (1 992 matched), the 413 book-like triaged with per-item
      evidence — A 98 · B 24 (+1 cand) · C-cand 14 · D-author 169 · D-third 62 · E 45; census:
      [nagari_attachment_rights_census.md](https://github.com/gasyoun/IndologyScholars/blob/main/nagari/reports/nagari_attachment_rights_census.md)
      + [rights CSV](https://github.com/gasyoun/IndologyScholars/blob/main/nagari/reports/nagari_attachment_rights.csv);
      B/C vote sheet: `review/indologyscholars-nagari-attachment-rights_17.07.26_review.html` (local).
- [ ] Optional: author-identity normalization (merge `tvitaly1`↔`Тихвинский
      Виталий`, `Renuvate`↔`Serge A`, etc.) — currently unmerged per guardrail.
      Adapt `Indology/indology_archive_research/cleaning.py`.
- [ ] Optional: static full-text search page over FTS5 (client-side is metadata-
      only; bodies live in DB/md). Gated behind H1104 — a search page over
      unredacted bodies would republish the 42 421 addresses. Mirror `Indology`
      `search.py` + `search.html`.
- [ ] Optional: matplotlib figures/ for a data-paper; datapackage.json + CITATION.cff.
- [ ] Optional: a linkable «Книгохранилище» index from `book_index.csv` — the
      *metadata* index is buildable now; **extracting the PDFs to
      `data/attachments/` is blocked** on the rights `@DECIDE` above, and the two
      must not be conflated: an index of what was posted is not a distribution of it.

## 🚧 Current Work-In-Progress (WIP)
- None. The retrospective page is **live**: https://gasyoun.github.io/IndologyScholars/nagari/
  (HTTP 200, 241 843 bytes, 0 third-party addresses). Next work is H1104.
- **22-07-2026 (Sonnet 5 `claude-sonnet-5`, H979 sub-task 1): `/publish-safety-check` run
  retroactively against the live page** (no prior explicit verdict on record). **Verdict:
  GO — page redaction holds, no fix needed.** Checked: (1) visibility — `IndologyScholars`
  is PUBLIC with Pages already active, consistent with the repo's existing posture, not a
  new decision; (2) rights — page renders only aggregate/derived data, no attachment blobs
  or quoted third-party bodies; (3) personal data —
  `python scripts/audit_publish_surface.py` re-run clean: `[page] OK - no third-party
  addresses found`; (4) secrets — none found; (5) gitignored-bulk leakage — `nagari/data/`
  (incl. the 1,796-file/986 MB `data/attachments/` extracted for the H1142 rights census)
  and `nagari/md/` (69.1 MB, full bodies) confirmed **never committed**
  (`git log --all -- nagari/data nagari/md` empty) and excluded from Pages by
  `.gitignore`'s `nagari/site/*` + `!nagari/site/index.html` allow-list, so neither can
  leak via the publish mechanism. The open attachment-rights `@DECIDE` above is unrelated
  to this page's safety and stays exactly as tracked (H1142 census + pending review-sheet
  vote). H979 sub-task 1 closes.

## 🧠 Dev Notes & Hypotheses
- Source order is newest-first; parse boundaries streamed by `^From ` (mailbox not
  used) so one malformed message can't abort — 0 parse errors on 18 729.
- Threading uses `X-GM-THRID` (Gmail thread id) → 2 928 threads; reply edges from
  `In-Reply-To` resolve 13 036 links.
- Key finding: message volume peaked 2015 (2 593), but thread COUNT keeps rising
  into 2025 (293) — long debates gave way to many short Q&A threads.
- IAST top terms are the traditional commentary apparatus (anvaya, pratipadārtha,
  padavibhāga, tātparya) — fingerprints the reading-group method.
- 367 book-like files (294 PDF, 216 MB): Knauer, Bühler, Śaṅkara-bhāṣyas, etc.
- Browser-pane screenshots time out on the SVG-heavy page; verified via JS DOM
  introspection instead (renderer limit, not a page bug).

## ✅ Completed (Recent only)
- mbox → SQLite+FTS5 ingest (18 729 msgs, 2 030 attachments, 2 333 members, 0 err).
- 4 analysis layers → CSV + site_data.json.
- Per-thread Markdown mirror (2 928 threads / 22 year-folders).
- Self-contained retrospective HTML (337 KB), validated data-viz palette, light/dark.
- Privacy: raw dump/DB/md/site/data git-ignored; page emails redacted, names masked.

{
  "description": "Reserved for redirects we explicitly want to preserve. Empty by default — orphan PERS_<hash> URLs from past dedup merges are not kept (validator will flag them as stale).",
  "redirects": {}
}

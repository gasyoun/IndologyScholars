"""vk_ors_archive — reproducible analysis of the VK public page/group behind
"Общество ревнителей санскрита" (vk.com/wall-88831040), 2015–2026.

Sibling in spirit to ``nagari/nagari_group_archive`` (the closed-group
Takeout archive) and ``Indology/indology_archive_research`` (the public
INDOLOGY-L atlas), but the source here is a VK wall export: one post per row
(text, engagement counters, attachment types), no thread/reply structure and
no per-author breakdown (the wall has a single publishing account). Raw data
(the xlsx export and the built SQLite DB) is never committed; only curated,
publish-gated outputs may be published.
"""

__version__ = "0.1.0"
